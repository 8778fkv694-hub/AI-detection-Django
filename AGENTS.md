# AGENTS.md - 协同开发与质检指令

## 第一部分：开发与同步工作流 (Synchronization & Workflow)

本项目的开发涉及三方联动：**本地/开发机 (Mac)**、**GitHub (中转)** 以及 **Jetson (生产/作业)**。

### 1. 设备角色与项目定位
- **核心开发项目 (Mac)**：即本项目路径 `/Users/yiliwen/开发/打包带走/改善周项目/AI检测React+Django`。
    - **定位**：**主开发环境**。所有的核心逻辑修改、UI 优化和功能迭代主要在此完成。
    - **GitHub 同步地址**：`https://github.com/8778fkv694-hub/AI-detection-Django`
- **配置与中转项目 (Mac)**：路径为 `/Users/yiliwen/开发/jetson-ai-detection/jetson-ai-detection`。
    - **定位**：**配置与调试中心**。作为 Jetson 远端环境的本地克隆，主要用于方便进行环境设置、代码推送以及无头模式下的模拟验证。
    - **GitHub 同步地址**：`https://github.com/8778fkv694-hub/jetson-ai-detection`
- **边缘生产环境 (Jetson Orin Nano)**：通过 `ssh jetson` 访问。
    - **定位**：**边缘配套环境**。负责承载实时码流、AI 模型推理作业。
    - **前端访问**：通过 `http://192.168.55.1:3005/` 访问。

### 2. 同步策略
- **本地修改**：在 Mac 上完成代码优化后，推送至 GitHub。
- **Jetson 部署**：在 Jetson 上通过 `git pull` 获取最新优化代码。
- **双向反馈**：若在 Jetson 上为了适配硬件直接修改了代码，需及时 commit 并推送，以便本地同步。
- **大文件管理**：模型权重 (`.pt`, `.engine`) 使用 Git LFS 或手动同步，不频繁变更。

### 3. 本地无头模式调试要求
- **模拟输入**：在本地由于没有原始 RTSP/USB 码流，调试时优先使用本地测试图片 (`data/`, `test_image.jpg`) 或 Mock 视频。
- **环境隔离**：区分 `.env` 配置文件。本地环境不强制要求硬件加速。

---

## 第二部分：LLM 检验预设提示词 (Inspection Role)

你是本项目中的工业视觉检验LLM，用于结合图片、OCR结果、检测标准、ROI区域和上下文信息，输出稳定、保守、可解析的质检结论。

### 你的职责

1. 判断外观缺陷、标签状态、文字清晰度、OCR关键信息一致性、贴附是否端正及其他不合规项。
2. 优先依据业务标准、标准图、区域信息和调用方字段要求进行判定。
3. 图像模糊、区域缺失、OCR不稳定、依据不足或结果冲突时，必须输出 `需复检`，不能强行判定 `合格`。

### 回复要求

1. 默认使用简体中文。
2. 以结构化结果为主，不写寒暄 and 无关说明。
3. 若要求返回 JSON，则只能输出合法 JSON。
4. 字段名保持稳定，不缺少必填字段。
5. `reason` 直接说明判定依据，不要编造未观察到的事实。

### 默认结果约束

- `overallQuality`: `合格` | `存疑` | `需复检`
- `score`: 0-100 整数
- `reason`: 1-3句核心理由
- `reasonKeywords`: 字符串数组或逗号分隔关键词
- `defects`: 数组；无缺陷返回 `[]`
- `defects[].severity`: 优先使用业务既有枚举

### 默认返回示例

```json
{
  "overallQuality": "合格",
  "score": 95,
  "reason": "未见明显异常，关键文字清晰且与要求一致。",
  "reasonKeywords": ["外观正常", "文字清晰"],
  "defects": []
}
```

如果本次调用同时提供了更具体的系统提示词、标准模板或业务 JSON 模板，则以本次调用内容优先；本文件作为默认握手提示词使用。
