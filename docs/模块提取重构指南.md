# 模块提取重构指南（已拆分）

> ⚠️ **注意**: 本文档已拆分为多个更小的文档，请查看 [模块提取重构指南/README.md](./模块提取重构指南/README.md)

## 📋 文档已拆分

本指南已拆分为以下文档，便于查阅：

1. **[README.md](./模块提取重构指南/README.md)** - 主索引和导航
2. **[01-通用模块提取指南.md](./模块提取重构指南/01-通用模块提取指南.md)** - 通用模块清单
3. **[02-超大页面拆分指南.md](./模块提取重构指南/02-超大页面拆分指南.md)** - 超大页面拆分计划
4. **[03-提取步骤详解.md](./模块提取重构指南/03-提取步骤详解.md)** - 详细提取步骤
5. **[04-风险分析与检查清单.md](./模块提取重构指南/04-风险分析与检查清单.md)** - 风险分析和检查清单
6. **[05-目标与统计.md](./模块提取重构指南/05-目标与统计.md)** - 目标和量化指标

**👉 请点击 [README.md](./模块提取重构指南/README.md) 查看完整指南**

---

## 📋 目录（旧版，已废弃）

> 以下内容已迁移到拆分后的文档中

---

## 🎯 高优先级模块清单

### 1. CSV 导出工具 (`src/lib/export/csvExporter.ts`)

**优先级**: ⭐⭐⭐⭐⭐ (最高)  
**风险等级**: 🟢 低  
**复杂度**: 低  
**影响文件**: 4 个结果页面  
**预计减少代码**: **~182 行**（每个页面约 45 行）

#### 涉及文件及行数统计

| 文件 | 代码行数 | 提取后减少 | 说明 |
|------|---------|-----------|------|
| `LiveInspectionResultsScreen.tsx` | 141-185 (45行) | **45行** | 9列，标准格式 |
| `BatchInspectionResultsScreen.tsx` | 144-188 (45行) | **45行** | 9列，标准格式 |
| `OCRInspectionResultsScreen.tsx` | 133-178 (46行) | **46行** | 10列，包含OCR列 |
| `OCRErrorPreventionResultsScreen.tsx` | 145-190 (46行) | **46行** | 10列，包含OCR列 |
| **总计** | **182行** | **~182行** | **4个页面** |

#### 重复代码
- `escapeCsvField` 函数（完全相同，8行）
- CSV 生成和下载逻辑（基本相同，30行）
- BOM 处理（完全相同，7行）

#### 差异点
- CSV 表头不同（9列 vs 10列）
- 字段映射不同（有些有 OCR，有些没有）
- 文件名前缀不同（"实时检测结果" vs "批量检测结果" vs "OCR检测结果" vs "OCR防呆检测结果"）

#### 提取后目标
- ✅ 创建共享模块：`src/lib/export/csvExporter.ts`（约 120 行）
- ✅ 每个页面减少：**~45 行**
- ✅ 总减少代码：**~182 行**
- ✅ 代码复用率：**100%**（完全消除重复）

---

### 2. YOLO 检测 Hook (`src/hooks/useYoloDetection.ts`)

**优先级**: ⭐⭐⭐⭐ (高)  
**风险等级**: 🟡 中  
**复杂度**: 高  
**影响文件**: 5 个检测页面  
**预计减少代码**: **~600 行**（每个页面约 120 行）

#### 涉及文件及行数统计

| 文件 | 代码行数 | 提取后减少 | 说明 |
|------|---------|-----------|------|
| `LiveInspectionScreen.tsx` | 769-920 (152行) | **~120行** | 完整YOLO检测+自动抓拍 |
| `OCRDetectionScreen.tsx` | 2111-2187 + 其他 (200行) | **~150行** | 实时检测+工作流模式 |
| `OCRErrorPreventionScreen.tsx` | 估计 (150行) | **~120行** | 类似OCRDetection |
| `KitMatchingScreen.tsx` | 363-414 (52行) | **~40行** | 简化版检测逻辑 |
| `SafetyEquipmentScreen.tsx` | 195-243 (49行) | **~40行** | 简化版检测逻辑 |
| **总计** | **~600行** | **~470行** | **5个页面** |

#### 重复代码
- YOLO 检测执行逻辑（canvas绘制+API调用，约30行）
- OR/AND 模式判断（约50行）
- 检测结果过滤（约20行）
- 超时处理（约20行）
- 检测状态管理（约20行）

#### 差异点
- 抓拍逻辑不同（自动AI vs 手动，约50行差异）
- 状态管理不同（useState vs zustand）
- 图像处理函数不同（compressImage, cropImageToROI）
- 回调函数不同（onDetect, onTriggerCapture）

#### 提取后目标
- ✅ 创建共享Hook：`src/hooks/useYoloDetection.ts`（约 200 行）
- ✅ 每个页面减少：**~100-150 行**（复杂页面）或 **~40 行**（简单页面）
- ✅ 总减少代码：**~470 行**
- ✅ 代码复用率：**~78%**（核心逻辑复用，抓拍逻辑保留在页面）

---

### 3. 摄像头管理 Hook (`src/hooks/useCamera.ts`)

**优先级**: ⭐⭐⭐⭐ (高)  
**风险等级**: 🔴 高  
**复杂度**: 很高  
**影响文件**: 5 个页面  
**预计减少代码**: **~879 行**（每个页面约 175 行）

#### 涉及文件及行数统计

| 文件 | 代码行数 | 提取后减少 | 说明 |
|------|---------|-----------|------|
| `LiveInspectionScreen.tsx` | 587-745 (159行) | **~140行** | 物理摄像头+流媒体 |
| `OCRDetectionScreen.tsx` | 1461-1671 (210行) | **~180行** | 包含startCamera+toggleCamera |
| `OCRErrorPreventionScreen.tsx` | 1425-1635 (210行) | **~180行** | 类似OCRDetection |
| `KitMatchingScreen.tsx` | 估计 (150行) | **~130行** | 标准实现 |
| `SafetyEquipmentScreen.tsx` | 估计 (150行) | **~130行** | 标准实现 |
| **总计** | **~879行** | **~760行** | **5个页面** |

#### 重复代码
- 摄像头开关逻辑（`toggleCamera`, `startCamera`，约80行）
- 流媒体播放器管理（StreamPlayer, HLSPlayer，约60行）
- 物理摄像头流管理（getUserMedia，约30行）
- 错误处理和用户提示（约20行）

#### 差异点
- 状态管理不同（`isYoloActive`, `isRealtimeActive`, `isMonitoring`, `isPpeActive`）
- 回调函数不同（`onStreamTaken` 的行为不同）
- 错误提示方式不同（`toast` vs `alert`）
- 设备列表变量名不同（`availableDevices` vs `videoDevices`）
- 多窗口支持（OCRDetectionScreen 有 windowId）

#### 提取后目标
- ✅ 创建共享Hook：`src/hooks/useCamera.ts`（约 300 行）
- ✅ 每个页面减少：**~130-180 行**
- ✅ 总减少代码：**~760 行**
- ✅ 代码复用率：**~86%**（核心逻辑复用，状态管理通过参数传入）

---

## 📦 中优先级模块清单（非完全通用但可复用）

### 4. 结果页面组件库 (`src/components/results/`)

**优先级**: ⭐⭐⭐ (中)  
**风险等级**: 🟢 低  
**复杂度**: 中  
**影响文件**: 5+ 个结果页面  
**预计减少代码**: **~800 行**（每个页面约 160 行）

#### 涉及文件及行数统计

| 文件 | 组件行数 | 提取后减少 | 说明 |
|------|---------|-----------|------|
| `LiveInspectionResultsScreen.tsx` | 254-561 (308行) | **~160行** | StatCard + ResultCard + DetailPanel + EmptyState |
| `OCRInspectionResultsScreen.tsx` | 247-528 (282行) | **~150行** | StatCard + ResultCard + DetailPanel + EmptyState |
| `OCRErrorPreventionResultsScreen.tsx` | 257-543 (287行) | **~150行** | StatCard + ResultCard + DetailPanel + EmptyState |
| `BatchInspectionResultsScreen.tsx` | 255-460 (206行) | **~150行** | StatCard + ResultCard + DetailPanel + EmptyState |
| `KitMatchingResultsScreen.tsx` | 估计 (200行) | **~150行** | 类似组件 |
| **总计** | **~1,283行** | **~760行** | **5个页面** |

#### 重复组件

1. **StatCard**（统计卡片）
   - 完全相同，约 10 行/页面
   - 5 个页面 × 10 行 = **50 行**

2. **ResultCard**（结果卡片）
   - 基本相同，只有 Badge 内容不同，约 60 行/页面
   - 5 个页面 × 60 行 = **300 行**

3. **DetailPanel**（详情面板）
   - 结构相同，内容展示略有差异，约 200 行/页面
   - 5 个页面 × 200 行 = **1,000 行**

4. **EmptyState**（空状态）
   - 基本相同，只有文字和链接不同，约 15 行/页面
   - 5 个页面 × 15 行 = **75 行**

#### 差异点

- **ResultCard**: Badge 显示内容不同（"标准检测" vs "融合检测" vs "防呆检测"）
- **DetailPanel**: OCR/LLM/二维码展示内容不同（有些页面有，有些没有）
- **EmptyState**: 文字和返回链接不同

#### 提取后目标

- ✅ 创建共享组件库：`src/components/results/`
  - `StatCard.tsx`（约 15 行）
  - `ResultCard.tsx`（约 80 行，支持自定义 Badge）
  - `DetailPanel.tsx`（约 250 行，支持条件渲染）
  - `EmptyState.tsx`（约 25 行，支持自定义文字和链接）
- ✅ 每个页面减少：**~150-160 行**
- ✅ 总减少代码：**~760 行**
- ✅ 代码复用率：**~90%**（通过 props 支持差异）

---

### 5. 结果页面逻辑 Hook (`src/hooks/useResultsPage.ts`)

**优先级**: ⭐⭐⭐ (中)  
**风险等级**: 🟢 低  
**复杂度**: 低  
**影响文件**: 5+ 个结果页面  
**预计减少代码**: **~250 行**（每个页面约 50 行）

#### 涉及文件及行数统计

| 文件 | 逻辑行数 | 提取后减少 | 说明 |
|------|---------|-----------|------|
| `LiveInspectionResultsScreen.tsx` | 55-139 (85行) | **~50行** | 过滤 + 删除 + 清空 + 详情 + ESC监听 |
| `OCRInspectionResultsScreen.tsx` | 55-131 (77行) | **~50行** | 同上 |
| `OCRErrorPreventionResultsScreen.tsx` | 55-143 (89行) | **~50行** | 同上 |
| `BatchInspectionResultsScreen.tsx` | 55-121 (67行) | **~50行** | 同上 |
| `KitMatchingResultsScreen.tsx` | 估计 (60行) | **~50行** | 类似逻辑 |
| **总计** | **~378行** | **~250行** | **5个页面** |

#### 重复逻辑

1. **结果过滤逻辑**（useEffect）
   - 基本相同，只有过滤条件不同，约 15 行/页面
   - 5 个页面 × 15 行 = **75 行**

2. **handleDeleteResult**（删除单条记录）
   - 完全相同，约 20 行/页面
   - 5 个页面 × 20 行 = **100 行**

3. **handleClearResults**（清空所有记录）
   - 基本相同，只有确认消息不同，约 25 行/页面
   - 5 个页面 × 25 行 = **125 行**

4. **handleViewDetail / handleCloseDetail**（详情面板控制）
   - 完全相同，约 5 行/页面
   - 5 个页面 × 5 行 = **25 行**

5. **ESC 键监听**（useEffect）
   - 完全相同，约 10 行/页面
   - 5 个页面 × 10 行 = **50 行**

#### 差异点

- **过滤条件**: 不同页面过滤不同的 `detectionType`
- **清空确认消息**: 不同页面显示不同的统计信息
- **清空逻辑**: 有些使用 `clearResultsByType`，有些直接循环删除

#### 提取后目标

- ✅ 创建共享Hook：`src/hooks/useResultsPage.ts`（约 150 行）
- ✅ 每个页面减少：**~50 行**
- ✅ 总减少代码：**~250 行**
- ✅ 代码复用率：**~85%**（通过配置支持差异）

---

### 6. 图像处理工具函数 (`src/lib/imageUtils/`)

**优先级**: ⭐⭐ (低)  
**风险等级**: 🟢 低  
**复杂度**: 低  
**影响文件**: 3+ 个检测页面  
**预计减少代码**: **~150 行**（每个页面约 50 行）

#### 涉及文件及行数统计

| 文件 | 函数行数 | 提取后减少 | 说明 |
|------|---------|-----------|------|
| `LiveInspectionScreen.tsx` | 379-521 (143行) | **~50行** | cropImageToROI 函数 |
| `OCRDetectionScreen.tsx` | 估计 (100行) | **~50行** | 图像处理相关函数 |
| `OCRErrorPreventionScreen.tsx` | 估计 (100行) | **~50行** | 图像处理相关函数 |
| **总计** | **~343行** | **~150行** | **3个页面** |

#### 重复代码

- **cropImageToROI**: 裁剪图像到 ROI 区域（约 50 行）
- **图像压缩逻辑**: 压缩图像大小（已部分提取，可能还有重复）
- **图像格式转换**: base64 ↔ File ↔ Blob 转换

#### 差异点

- 不同页面使用的压缩参数不同
- ROI 裁剪逻辑略有差异

#### 提取后目标

- ✅ 创建共享工具：`src/lib/imageUtils/roiCropper.ts`（约 80 行）
- ✅ 每个页面减少：**~50 行**
- ✅ 总减少代码：**~150 行**
- ✅ 代码复用率：**~80%**

---

## 🔥 页面内部模块提取（超大页面专用）

> **目标**: 纯粹为了减少页面行数，即使模块只在当前页面使用，也要提取出来  
> **适用场景**: 页面超过 2000 行，代码难以维护

### 📏 超大页面判定标准

| 行数范围 | 等级 | 建议操作 | 优先级 |
|---------|------|---------|--------|
| **< 1000 行** | ✅ 正常 | 无需拆分 | - |
| **1000 - 2000 行** | ⚠️ 较大 | 可考虑拆分，非必需 | 低 |
| **2000 - 3000 行** | 🔶 大 | **建议拆分**，提取重复逻辑 | 中 |
| **3000 - 5000 行** | 🔴 超大 | **强烈建议拆分**，必须提取模块 | 高 |
| **> 5000 行** | 🚨 严重超大 | **立即拆分**，优先处理 | 最高 |

### 📊 当前项目页面行数统计

| 页面文件 | 当前行数 | 等级 | 建议目标行数 | 优先级 |
|---------|---------|------|------------|--------|
| `OCRDetectionScreen.tsx` | **6,331 行** | 🚨 严重超大 | < 2,000 行 | ⭐⭐⭐⭐⭐ |
| `OCRErrorPreventionScreen.tsx` | **5,915 行** | 🚨 严重超大 | < 2,000 行 | ⭐⭐⭐⭐⭐ |
| `KitMatchingScreen.tsx` | **3,684 行** | 🔴 超大 | < 2,000 行 | ⭐⭐⭐⭐ |
| `LiveInspectionScreen.tsx` | **2,610 行** | 🔴 超大 | < 1,500 行 | ⭐⭐⭐ |
| `ModelManagementScreen.tsx` | 875 行 | ✅ 正常 | - | - |
| `LocalModelScreen.tsx` | 1,027 行 | ⚠️ 较大 | - | - |

**判定标准**: **≥ 2000 行** 算超大页面，需要优先处理

### OCRDetectionScreen.tsx（6331 行 → 目标：< 2000 行）

#### 可提取模块清单

| 模块 | 类型 | 当前行数 | 提取后减少 | 说明 |
|------|------|---------|-----------|------|
| **配置和常量** | 文件 | ~150行 | **~150行** | PREPROCESSING_PRESETS, sliderStyles, 类型定义 |
| **QRCodeDetectionResult 组件** | 组件 | ~70行 | **~70行** | 二维码检测结果展示组件 |
| **图像处理 Hook** | Hook | ~200行 | **~200行** | 智能预处理、图像质量分析 |
| **OCR 处理 Hook** | Hook | ~300行 | **~300行** | processCapturedImage, performKeywordAnalysis |
| **融合模式 AI Hook** | Hook | ~100行 | **~100行** | performFusionAIAnalysis |
| **摄像头管理 Hook** | Hook | ~250行 | **~250行** | startCamera, toggleCamera, switchCamera |
| **实时检测 Hook** | Hook | ~200行 | **~200行** | performRealtimeDetection |
| **工作流管理 Hook** | Hook | ~150行 | **~150行** | 工作流状态管理、手动抓拍 |
| **设置面板组件** | 组件 | ~800行 | **~800行** | 所有设置面板 UI |
| **结果展示组件** | 组件 | ~600行 | **~600行** | OCR结果、历史记录展示 |
| **检测目标选择组件** | 组件 | ~400行 | **~400行** | 目标分组、选择 UI |
| **统计信息组件** | 组件 | ~200行 | **~200行** | 检测统计展示 |
| **总计** | - | **~3,420行** | **~3,420行** | **提取后页面约 2900 行** |

#### 详细提取计划

##### 1. 配置和常量文件 (`src/screens/ocrDetection/config.ts`)

**预计减少**: ~150 行

```typescript
// 提取内容：
- PREPROCESSING_PRESETS 配置（约 50 行）
- sliderStyles CSS（约 20 行）
- TestResult, BarcodeResult, OCRResult 类型定义（约 80 行）
```

##### 2. QRCodeDetectionResult 组件 (`src/components/ocr/QRCodeDetectionResult.tsx`)

**预计减少**: ~70 行

```typescript
// 提取内容：
- QRCodeDetectionResult 组件（179-250行）
```

##### 3. 图像处理 Hook (`src/hooks/ocr/useImagePreprocessing.ts`)

**预计减少**: ~200 行

```typescript
// 提取内容：
- performSmartImageAnalysis（约 50 行）
- applySmartPreprocessing（约 50 行）
- 图像质量分析相关状态和逻辑（约 100 行）
```

##### 4. OCR 处理 Hook (`src/hooks/ocr/useOCRProcessing.ts`)

**预计减少**: ~300 行

```typescript
// 提取内容：
- processCapturedImage（约 200 行）
- performKeywordAnalysis（约 30 行）
- performBarcodeDetection（约 20 行）
- 相关状态管理（约 50 行）
```

##### 5. 融合模式 AI Hook (`src/hooks/ocr/useFusionAI.ts`)

**预计减少**: ~100 行

```typescript
// 提取内容：
- performFusionAIAnalysis（约 90 行）
- 相关状态管理（约 10 行）
```

##### 6. 摄像头管理 Hook (`src/hooks/ocr/useOCRCamera.ts`)

**预计减少**: ~250 行

```typescript
// 提取内容：
- startCamera（约 150 行）
- toggleCamera（约 30 行）
- switchCamera（约 20 行）
- getAvailableDevices（约 30 行）
- 摄像头流监听逻辑（约 20 行）
```

##### 7. 实时检测 Hook (`src/hooks/ocr/useRealtimeDetection.ts`)

**预计减少**: ~200 行

```typescript
// 提取内容：
- performRealtimeDetection（约 150 行）
- 检测统计更新逻辑（约 30 行）
- 检测结果绘制逻辑（约 20 行）
```

##### 8. 工作流管理 Hook (`src/hooks/ocr/useOCRWorkflow.ts`)

**预计减少**: ~150 行

```typescript
// 提取内容：
- handleManualCapture（约 100 行）
- 工作流状态管理（约 30 行）
- 保存检测结果逻辑（约 20 行）
```

##### 9. 设置面板组件 (`src/components/ocr/OCRSettingsPanel.tsx`)

**预计减少**: ~800 行

```typescript
// 提取内容：
- 所有设置面板 UI（关键词、二维码、ROI、YOLO等）
- 约 3000-5000 行的 UI 代码
```

##### 10. 结果展示组件 (`src/components/ocr/OCRResultDisplay.tsx`)

**预计减少**: ~600 行

```typescript
// 提取内容：
- OCR 结果展示 UI（约 300 行）
- 历史记录展示 UI（约 300 行）
```

##### 11. 检测目标选择组件 (`src/components/ocr/TargetSelectionPanel.tsx`)

**预计减少**: ~400 行

```typescript
// 提取内容：
- 目标分组展示（约 200 行）
- 目标选择 UI（约 200 行）
```

##### 12. 统计信息组件 (`src/components/ocr/DetectionStats.tsx`)

**预计减少**: ~200 行

```typescript
// 提取内容：
- 检测统计展示 UI（约 200 行）
```

#### 提取后效果

- ✅ **原始行数**: 6,331 行
- ✅ **提取后行数**: 约 2,900 行
- ✅ **减少行数**: **~3,420 行**（减少 54%）
- ✅ **可维护性**: 大幅提升（每个模块职责单一）
- ✅ **可测试性**: 大幅提升（可以单独测试每个模块）

---

### OCRErrorPreventionScreen.tsx（5915 行 → 目标：< 2000 行）

**等级**: 🚨 严重超大  
**优先级**: ⭐⭐⭐⭐⭐（最高）

#### 可提取模块清单

| 模块 | 类型 | 当前行数 | 提取后减少 | 说明 |
|------|------|---------|-----------|------|
| **配置和常量** | 文件 | ~100行 | **~100行** | 类型定义、常量 |
| **图像处理 Hook** | Hook | ~200行 | **~200行** | 图像预处理逻辑 |
| **OCR 处理 Hook** | Hook | ~250行 | **~250行** | processCapturedImage |
| **摄像头管理 Hook** | Hook | ~250行 | **~250行** | 摄像头相关逻辑 |
| **实时检测 Hook** | Hook | ~200行 | **~200行** | 实时检测逻辑 |
| **设置面板组件** | 组件 | ~700行 | **~700行** | 设置面板 UI |
| **结果展示组件** | 组件 | ~500行 | **~500行** | 结果展示 UI |
| **总计** | - | **~2,200行** | **~2,200行** | **提取后页面约 3700 行** |

---

### KitMatchingScreen.tsx（3684 行 → 目标：< 2000 行）

**等级**: 🔴 超大  
**优先级**: ⭐⭐⭐⭐（高）

#### 可提取模块清单（待分析）

| 模块 | 类型 | 预计减少 | 说明 |
|------|------|---------|------|
| **配置和常量** | 文件 | ~100行 | 类型定义、常量 |
| **图像处理 Hook** | Hook | ~150行 | 图像处理逻辑 |
| **检测逻辑 Hook** | Hook | ~200行 | 检测执行逻辑 |
| **摄像头管理 Hook** | Hook | ~200行 | 摄像头相关逻辑 |
| **设置面板组件** | 组件 | ~600行 | 设置面板 UI |
| **结果展示组件** | 组件 | ~500行 | 结果展示 UI |
| **总计** | - | **~1,750行** | **提取后页面约 1900 行** |

---

### LiveInspectionScreen.tsx（2610 行 → 目标：< 1500 行）

**等级**: 🔴 超大  
**优先级**: ⭐⭐⭐（中）

#### 可提取模块清单（待分析）

| 模块 | 类型 | 预计减少 | 说明 |
|------|------|---------|------|
| **YOLO 检测 Hook** | Hook | ~150行 | YOLO检测逻辑（已在通用模块中） |
| **摄像头管理 Hook** | Hook | ~160行 | 摄像头相关逻辑（已在通用模块中） |
| **图像处理 Hook** | Hook | ~150行 | 图像压缩、ROI裁剪 |
| **设置面板组件** | 组件 | ~400行 | 设置面板 UI |
| **结果展示组件** | 组件 | ~300行 | 结果展示 UI |
| **总计** | - | **~1,160行** | **提取后页面约 1450 行** |

#### 可提取模块清单

| 模块 | 类型 | 当前行数 | 提取后减少 | 说明 |
|------|------|---------|-----------|------|
| **配置和常量** | 文件 | ~100行 | **~100行** | 类型定义、常量 |
| **图像处理 Hook** | Hook | ~200行 | **~200行** | 图像预处理逻辑 |
| **OCR 处理 Hook** | Hook | ~250行 | **~250行** | processCapturedImage |
| **摄像头管理 Hook** | Hook | ~250行 | **~250行** | 摄像头相关逻辑 |
| **实时检测 Hook** | Hook | ~200行 | **~200行** | 实时检测逻辑 |
| **设置面板组件** | 组件 | ~700行 | **~700行** | 设置面板 UI |
| **结果展示组件** | 组件 | ~500行 | **~500行** | 结果展示 UI |
| **总计** | - | **~2,200行** | **~2,200行** | **提取后页面约 3700 行** |

---

## ⚠️ 风险分析

### CSV 导出工具 (`csvExporter.ts`)

#### 风险点

1. **CSV 格式差异**
   - 风险：不同页面需要不同的列
   - 影响：中等
   - 解决方案：使用配置对象，支持自定义列映射

2. **字段映射差异**
   - 风险：有些页面需要 `ocrResult`，有些不需要
   - 影响：低
   - 解决方案：通过 `getRowData` 函数自定义字段提取

3. **文件名格式**
   - 风险：文件名前缀不同
   - 影响：低
   - 解决方案：作为配置参数传入

#### 风险等级：🟢 低

**原因**：
- 逻辑简单，依赖少
- 不影响核心功能
- 容易测试和验证
- 容易回滚

---

### YOLO 检测 Hook (`useYoloDetection.ts`)

#### 风险点

1. **状态依赖过多**
   - 风险：依赖 15+ 个状态和 setter
   - 影响：高
   - 解决方案：使用配置对象封装，通过回调处理状态更新

2. **抓拍逻辑差异**
   - 风险：不同页面的抓拍逻辑完全不同
   - 影响：高
   - 解决方案：Hook 只负责检测，抓拍逻辑通过回调传入

3. **图像处理函数依赖**
   - 风险：依赖 `compressImage`, `cropImageToROI` 等外部函数
   - 影响：中
   - 解决方案：通过参数传入这些函数

4. **检测结果处理差异**
   - 风险：不同页面处理检测结果的方式不同
   - 影响：中
   - 解决方案：通过回调函数处理检测结果

#### 风险等级：🟡 中

**原因**：
- 核心功能，影响面大
- 状态依赖复杂
- 需要充分测试

**建议**：
- 分阶段提取：先提取纯检测逻辑，抓拍逻辑保留在页面
- 充分测试每个使用该 Hook 的页面

---

### 摄像头管理 Hook (`useCamera.ts`)

#### 风险点

1. **状态管理不一致**
   - 风险：不同页面使用不同的状态管理方式
   - 影响：高
   - 解决方案：统一接口，通过参数传入状态 setter

2. **依赖的 Ref 和外部实例**
   - 风险：需要访问多个 ref（videoRef, streamPlayerRef, hlsPlayerRef）
   - 影响：高
   - 解决方案：通过参数传入所有必需的 ref

3. **回调函数差异**
   - 风险：不同页面的回调行为不同
   - 影响：中
   - 解决方案：通过配置对象传入回调函数

4. **设备列表变量名不一致**
   - 风险：`availableDevices` vs `videoDevices`
   - 影响：低
   - 解决方案：统一变量名，或在 Hook 内部管理

5. **物理摄像头约束配置不同**
   - 风险：约束配置略有差异
   - 影响：低
   - 解决方案：通过配置对象传入约束参数

#### 风险等级：🔴 高

**原因**：
- 复杂度最高
- 依赖最多
- 影响面最广
- 错误处理复杂

**建议**：
- 最后提取
- 分步提取：先提取物理摄像头，再提取流媒体
- 充分测试每个使用场景

---

## 📝 提取步骤

### 阶段 1: CSV 导出工具（推荐起点）

#### 步骤 1.1: 创建模块文件

创建 `src/lib/export/csvExporter.ts`

**注意事项**：
- ✅ 保持函数签名简单
- ✅ 使用 TypeScript 严格类型
- ✅ 提供预设配置
- ✅ 支持自定义配置

#### 步骤 1.2: 从单个页面开始

**推荐页面**: `LiveInspectionResultsScreen.tsx`

**原因**：
- CSV 格式最简单（9列）
- 依赖最少
- 独立性强

**操作**：
1. 创建 `csvExporter.ts` 文件
2. 提取 `escapeCsvField` 函数
3. 提取 CSV 生成和下载逻辑
4. 创建预设配置 `liveInspectionCSVConfig`
5. 在页面中使用新模块
6. **保留原代码**（注释掉），用于对比
7. 测试验证
8. 确认无误后删除原代码

#### 步骤 1.3: 应用到其他页面

按顺序应用：
1. `BatchInspectionResultsScreen.tsx` - 格式相同，可直接复用
2. `OCRInspectionResultsScreen.tsx` - 需要添加 OCR 列配置
3. `OCRErrorPreventionResultsScreen.tsx` - 需要添加 OCR 和 LLM 列配置
4. `CleanroomInspectionResultsScreen.tsx` - 格式不同，需要自定义配置

#### 步骤 1.4: 验证清单

- [ ] 导出空结果时正确提示
- [ ] 导出有数据的结果，CSV 文件正确
- [ ] Excel 打开 CSV，中文显示正常
- [ ] 特殊字符（逗号、引号、换行）正确处理
- [ ] 文件名格式正确
- [ ] 成功提示消息正确
- [ ] 错误处理正常

---

### 阶段 2: YOLO 检测 Hook（核心功能）

#### 步骤 2.1: 设计 Hook 接口

**建议接口**：

```typescript
interface YoloDetectionConfig {
  // 必需参数
  videoRef: RefObject<HTMLVideoElement>;
  isActive: boolean;
  detectionConfidence: number;
  selectedTargets: string[];
  
  // 检测模式
  mode: 'or' | 'and';
  timeoutSeconds?: number;
  
  // 回调函数
  onDetect: (detections: BackendYoloDetection[]) => void;
  onTriggerCapture?: (image: string, detections: BackendYoloDetection[]) => void;
  
  // 可选配置
  autoCapture?: boolean;
  imageSaveMode?: 'full' | 'roi';
  compressImage?: (image: string) => Promise<string>;
  cropImageToROI?: (image: string, detections: BackendYoloDetection[]) => Promise<string>;
}
```

#### 步骤 2.2: 分阶段提取

**第一阶段：纯检测逻辑**
- 只提取检测执行逻辑
- 不包含抓拍逻辑
- 通过回调返回检测结果

**第二阶段：检测模式逻辑**
- 提取 OR/AND 模式判断
- 提取超时处理
- 提取检测元素状态管理

**第三阶段：整合**
- 整合所有逻辑
- 优化性能
- 添加错误处理

#### 步骤 2.3: 从单个页面开始

**推荐页面**: `LiveInspectionScreen.tsx`

**原因**：
- 逻辑相对简单
- 状态管理清晰
- 有自动 AI 检测功能

**操作**：
1. 创建 `useYoloDetection.ts` Hook
2. 先提取纯检测逻辑（不包含抓拍）
3. 在页面中使用新 Hook
4. **保留原代码**，用于对比
5. 测试验证检测功能
6. 确认无误后逐步提取抓拍逻辑

#### 步骤 2.4: 应用到其他页面

按复杂度顺序：
1. `LiveInspectionScreen.tsx` - 最简单
2. `SafetyEquipmentScreen.tsx` - 中等复杂度
3. `KitMatchingScreen.tsx` - 中等复杂度
4. `OCRDetectionScreen.tsx` - 最复杂（工作流模式）

#### 步骤 2.5: 验证清单

- [ ] YOLO 检测正常执行
- [ ] OR 模式正常工作
- [ ] AND 模式正常工作
- [ ] 超时处理正常
- [ ] 检测结果正确更新
- [ ] 自动抓拍功能正常（如果启用）
- [ ] 性能没有下降
- [ ] 错误处理正常

---

### 阶段 3: 摄像头管理 Hook（最复杂）

#### 步骤 3.1: 设计 Hook 接口

**建议接口**：

```typescript
interface CameraConfig {
  // 必需参数
  videoRef: RefObject<HTMLVideoElement>;
  availableDevices: CameraDevice[];
  selectedDeviceId: string;
  windowId: string;
  
  // 状态管理
  isCameraOn: boolean;
  setIsCameraOn: (value: boolean) => void;
  
  // 可选状态（不同页面不同）
  onCameraStateChange?: (isOn: boolean) => void;
  onStreamEnded?: () => void;
  
  // 回调函数
  callbacks?: {
    onStreamTaken?: () => void;
    onError?: (error: Error) => void;
    onSuccess?: (message: string) => void;
  };
  
  // 配置
  constraints?: MediaStreamConstraints;
  hlsConfig?: HLSStreamConfig;
}
```

#### 步骤 3.2: 分步提取

**第一步：物理摄像头管理**
- 提取物理摄像头启动/关闭逻辑
- 提取错误处理
- 提取流状态监听

**第二步：流媒体管理**
- 提取 StreamPlayer 管理
- 提取 HLSPlayer 管理
- 提取流媒体错误处理

**第三步：整合**
- 整合物理摄像头和流媒体
- 统一错误处理
- 优化代码结构

#### 步骤 3.3: 从单个页面开始

**推荐页面**: `LiveInspectionScreen.tsx`

**原因**：
- 摄像头逻辑相对完整
- 包含物理摄像头和流媒体
- 错误处理较完善

**操作**：
1. 创建 `useCamera.ts` Hook
2. 先提取物理摄像头管理
3. 再提取流媒体管理
4. 在页面中使用新 Hook
5. **保留原代码**，用于对比
6. 测试验证所有功能
7. 确认无误后删除原代码

#### 步骤 3.4: 应用到其他页面

按复杂度顺序：
1. `LiveInspectionScreen.tsx` - 标准实现
2. `SafetyEquipmentScreen.tsx` - 有兼容性处理
3. `KitMatchingScreen.tsx` - 类似实现
4. `OCRDetectionScreen.tsx` - 最复杂（多窗口支持）

#### 步骤 3.5: 验证清单

- [ ] 物理摄像头启动正常
- [ ] 物理摄像头关闭正常
- [ ] 流媒体摄像头启动正常（HLS）
- [ ] 流媒体摄像头启动正常（JPEG）
- [ ] 摄像头切换正常
- [ ] 多窗口支持正常
- [ ] 错误处理正常（权限、占用等）
- [ ] 流状态监听正常
- [ ] 性能没有下降

---

## ✅ 检查清单

### 提取前检查

- [ ] **列出所有依赖**
  - [ ] 状态变量（useState, zustand）
  - [ ] Ref 对象（useRef）
  - [ ] 外部函数依赖
  - [ ] 外部库依赖

- [ ] **识别差异点**
  - [ ] 不同页面的实现差异
  - [ ] 状态管理方式差异
  - [ ] 回调函数差异
  - [ ] 错误处理差异

- [ ] **设计统一接口**
  - [ ] 定义配置对象类型
  - [ ] 确定必需参数
  - [ ] 确定可选参数
  - [ ] 设计回调函数接口

- [ ] **确定提取策略**
  - [ ] 分阶段提取计划
  - [ ] 测试验证计划
  - [ ] 回滚方案

### 提取时注意

- [ ] **保持向后兼容**
  - [ ] 先不删除原代码（注释掉）
  - [ ] 新旧代码并行运行对比
  - [ ] 逐步迁移

- [ ] **类型安全**
  - [ ] 完整的 TypeScript 类型定义
  - [ ] 严格的类型检查
  - [ ] 避免 `any` 类型

- [ ] **提供默认值**
  - [ ] 可选参数有默认值
  - [ ] 回调函数有默认实现
  - [ ] 配置对象有预设

- [ ] **错误处理**
  - [ ] 统一的错误处理逻辑
  - [ ] 详细的错误信息
  - [ ] 用户友好的错误提示

- [ ] **性能优化**
  - [ ] 避免不必要的重新渲染
  - [ ] 使用 useCallback/useMemo
  - [ ] 优化依赖数组

### 提取后验证

- [ ] **功能验证**
  - [ ] 所有功能正常工作
  - [ ] 边界情况处理正确
  - [ ] 错误情况处理正确

- [ ] **代码质量**
  - [ ] 无 TypeScript 错误
  - [ ] 无 Lint 警告
  - [ ] 代码风格一致

- [ ] **性能验证**
  - [ ] 性能没有下降
  - [ ] 内存使用正常
  - [ ] 无内存泄漏

- [ ] **兼容性验证**
  - [ ] 所有使用该模块的页面正常
  - [ ] 不同浏览器测试
  - [ ] 不同设备测试

---

## ❓ 常见问题

### Q1: 如何决定哪些代码应该提取？

**A**: 遵循以下原则：
1. **重复度**：3个以上文件有相同代码
2. **复杂度**：代码逻辑复杂，值得复用
3. **独立性**：功能相对独立，依赖少
4. **维护性**：提取后更容易维护

### Q2: 如何处理不同页面的差异？

**A**: 使用以下策略：
1. **配置对象**：通过配置参数处理差异
2. **回调函数**：页面特定逻辑通过回调传入
3. **预设配置**：为常见场景提供预设
4. **可选参数**：非必需功能作为可选参数

### Q3: 提取后如何测试？

**A**: 测试步骤：
1. **单元测试**：测试提取的模块本身
2. **集成测试**：测试模块在页面中的使用
3. **功能测试**：测试完整功能流程
4. **回归测试**：确保没有破坏现有功能

### Q4: 如果提取后出现问题怎么办？

**A**: 回滚策略：
1. **保留原代码**：提取时先注释原代码，不删除
2. **版本控制**：提交前创建分支
3. **逐步迁移**：一个页面一个页面迁移
4. **快速回滚**：出问题时可以快速恢复

### Q5: 如何确保类型安全？

**A**: 类型安全措施：
1. **严格类型**：避免使用 `any`
2. **接口定义**：为所有配置对象定义接口
3. **类型检查**：启用 TypeScript 严格模式
4. **类型测试**：确保类型推断正确

### Q6: 性能会受影响吗？

**A**: 性能考虑：
1. **避免过度抽象**：不要为了提取而提取
2. **使用 useCallback/useMemo**：避免不必要的重新计算
3. **优化依赖数组**：减少不必要的重新渲染
4. **性能测试**：提取前后对比性能

---

## 📊 提取优先级矩阵与目标统计

| 模块 | 优先级 | 风险 | 复杂度 | 影响页面 | 预计减少行数 | 建议顺序 |
|------|--------|------|--------|----------|-------------|----------|
| CSV 导出工具 | ⭐⭐⭐⭐⭐ | 🟢 低 | 低 | 4个页面 | **~182行** | 1️⃣ 第一个 |
| YOLO 检测 Hook | ⭐⭐⭐⭐ | 🟡 中 | 高 | 5个页面 | **~470行** | 2️⃣ 第二个 |
| 摄像头管理 Hook | ⭐⭐⭐⭐ | 🔴 高 | 很高 | 5个页面 | **~760行** | 3️⃣ 第三个 |
| 结果页面组件库 | ⭐⭐⭐ | 🟢 低 | 中 | 5个页面 | **~760行** | 4️⃣ 第四个 |
| 结果页面逻辑 Hook | ⭐⭐⭐ | 🟢 低 | 低 | 5个页面 | **~250行** | 5️⃣ 第五个 |
| 图像处理工具函数 | ⭐⭐ | 🟢 低 | 低 | 3个页面 | **~150行** | 6️⃣ 第六个 |
| **总计（高优先级）** | - | - | - | **14个页面** | **~1,412行** | - |
| **总计（全部通用模块）** | - | - | - | **27个页面** | **~2,572行** | - |
| **页面内部模块（OCRDetection）** | ⭐⭐⭐⭐⭐ | 🟢 低 | 中 | 1个页面 | **~3,420行** | 7️⃣ 超大页面优先 |
| **页面内部模块（OCRErrorPrevention）** | ⭐⭐⭐⭐⭐ | 🟢 低 | 中 | 1个页面 | **~2,200行** | 8️⃣ 超大页面优先 |
| **页面内部模块（KitMatching）** | ⭐⭐⭐⭐ | 🟢 低 | 中 | 1个页面 | **~1,750行** | 9️⃣ 超大页面 |
| **页面内部模块（LiveInspection）** | ⭐⭐⭐ | 🟢 低 | 中 | 1个页面 | **~1,160行** | 🔟 超大页面 |
| **总计（包含页面内部）** | - | - | - | **31个页面** | **~9,102行** | - |

### 📈 总体目标

#### 高优先级模块（前3个）
- ✅ **总减少代码行数**: **~1,412 行**
- ✅ **影响页面数**: **14 个页面**（部分页面会使用多个模块）
- ✅ **代码复用率**: **80-90%**（核心逻辑复用）
- ✅ **维护成本**: **降低 70%**（修改一处，所有页面生效）
- ✅ **新页面开发**: **节省 50% 时间**（直接使用共享模块）

#### 全部模块（包含中优先级）
- ✅ **总减少代码行数**: **~2,572 行**
- ✅ **影响页面数**: **27 个页面**（部分页面会使用多个模块）
- ✅ **代码复用率**: **80-90%**（核心逻辑复用）
- ✅ **维护成本**: **降低 75%**（修改一处，所有页面生效）
- ✅ **新页面开发**: **节省 60% 时间**（直接使用共享模块）

#### 页面内部模块提取（超大页面）
- ✅ **OCRDetectionScreen 减少**: **~3,420 行**（6,331 → 2,900 行，减少 54%）
- ✅ **OCRErrorPreventionScreen 减少**: **~2,200 行**（5,915 → 3,700 行，减少 37%）
- ✅ **KitMatchingScreen 减少**: **~1,750 行**（3,684 → 1,900 行，减少 47%）
- ✅ **LiveInspectionScreen 减少**: **~1,160 行**（2,610 → 1,450 行，减少 44%）
- ✅ **总减少代码行数**: **~8,530 行**（仅页面内部提取）
- ✅ **可维护性**: **大幅提升**（每个模块职责单一，易于理解和修改）
- ✅ **可测试性**: **大幅提升**（可以单独测试每个模块）

### 🎯 分阶段目标

#### 阶段 1: CSV 导出工具（第1周）
- **目标**: 减少 **~182 行**代码
- **影响**: 4 个结果页面
- **风险**: 🟢 低
- **预计时间**: 2-3 小时

#### 阶段 2: YOLO 检测 Hook（第2周）
- **目标**: 减少 **~470 行**代码
- **影响**: 5 个检测页面
- **风险**: 🟡 中
- **预计时间**: 1-2 天

#### 阶段 3: 摄像头管理 Hook（第3周）
- **目标**: 减少 **~760 行**代码
- **影响**: 5 个页面
- **风险**: 🔴 高
- **预计时间**: 2-3 天

#### 阶段 4: 结果页面组件库（第4周）
- **目标**: 减少 **~760 行**代码
- **影响**: 5 个结果页面
- **风险**: 🟢 低
- **预计时间**: 1-2 天

#### 阶段 5: 结果页面逻辑 Hook（第4周）
- **目标**: 减少 **~250 行**代码
- **影响**: 5 个结果页面
- **风险**: 🟢 低
- **预计时间**: 0.5-1 天

#### 阶段 6: 图像处理工具函数（第5周）
- **目标**: 减少 **~150 行**代码
- **影响**: 3 个检测页面
- **风险**: 🟢 低
- **预计时间**: 0.5 天

#### 阶段 7: OCRDetectionScreen 页面内部模块提取（第6-7周）
- **目标**: 减少 **~3,420 行**代码（6,331 → 2,900 行）
- **影响**: 1 个超大页面
- **风险**: 🟢 低（页面专用，不影响其他页面）
- **预计时间**: 3-5 天
- **优先级**: ⭐⭐⭐⭐⭐（超大页面优先）

#### 阶段 8: OCRErrorPreventionScreen 页面内部模块提取（第8周）
- **目标**: 减少 **~2,200 行**代码（5,915 → 3,700 行）
- **影响**: 1 个超大页面
- **风险**: 🟢 低（页面专用，不影响其他页面）
- **预计时间**: 2-3 天
- **优先级**: ⭐⭐⭐⭐⭐（超大页面优先）

#### 阶段 9: KitMatchingScreen 页面内部模块提取（第9周）
- **目标**: 减少 **~1,750 行**代码（3,684 → 1,900 行）
- **影响**: 1 个超大页面
- **风险**: 🟢 低（页面专用，不影响其他页面）
- **预计时间**: 2-3 天
- **优先级**: ⭐⭐⭐⭐（超大页面）

#### 阶段 10: LiveInspectionScreen 页面内部模块提取（第10周）
- **目标**: 减少 **~1,160 行**代码（2,610 → 1,450 行）
- **影响**: 1 个超大页面
- **风险**: 🟢 低（页面专用，不影响其他页面）
- **预计时间**: 1-2 天
- **优先级**: ⭐⭐⭐（超大页面）

---

## 🎯 成功标准与量化目标

### 代码质量目标
- ✅ **减少重复代码**: **~1,412 行**（量化目标）
- ✅ **代码复用率**: **80-90%**（核心逻辑）
- ✅ **所有模块有完整的 TypeScript 类型**: 100% 类型覆盖
- ✅ **无 Lint 错误和警告**: 0 错误，0 警告
- ✅ **代码可读性和可维护性提升**: 模块化，单一职责

### 功能完整性目标
- ✅ **所有功能正常工作**: 100% 功能保持
- ✅ **性能没有下降**: 性能指标保持或提升
- ✅ **错误处理完善**: 统一的错误处理机制
- ✅ **用户体验保持一致**: UI/UX 无变化

### 可复用性目标
- ✅ **新页面可以直接使用共享模块**: 开箱即用
- ✅ **配置灵活，支持不同场景**: 通过配置对象支持差异
- ✅ **文档完善，易于理解**: 每个模块有完整文档

### 量化指标

| 指标 | 目标值 | 当前值 | 完成度 |
|------|--------|--------|--------|
| 减少代码行数（高优先级） | ~1,412 行 | 0 行 | 0% |
| 减少代码行数（全部通用模块） | ~2,572 行 | 0 行 | 0% |
| 减少代码行数（页面内部） | ~8,530 行 | 0 行 | 0% |
| 减少代码行数（总计） | **~11,102 行** | 0 行 | 0% |
| 模块数量（高优先级） | 3 个 | 0 个 | 0% |
| 模块数量（全部通用模块） | 6 个 | 0 个 | 0% |
| 模块数量（页面内部） | 12+ 个 | 0 个 | 0% |
| 影响页面数（高优先级） | 14 个 | 0 个 | 0% |
| 影响页面数（全部通用模块） | 27 个 | 0 个 | 0% |
| 影响页面数（页面内部） | 4 个超大页面 | 0 个 | 0% |
| 超大页面行数（OCRDetection） | 2,900 行 | 6,331 行 | - |
| 超大页面行数（OCRErrorPrevention） | 3,700 行 | 5,915 行 | - |
| 超大页面行数（KitMatching） | 1,900 行 | 3,684 行 | - |
| 超大页面行数（LiveInspection） | 1,450 行 | 2,610 行 | - |
| 代码复用率 | 80-90% | 0% | 0% |
| 类型覆盖率 | 100% | - | - |
| Lint 错误数 | 0 | - | - |

---

## 📚 参考资源

### 相关文件
- `src/lib/barcode/barcodeAnalyzer.ts` - 二维码检测模块（已提取）
- `src/lib/roi/roiStitcher.ts` - ROI 拼接模块（已提取）
- `src/lib/ocr/keywordAnalyzer.ts` - 关键词分析模块（已提取）

### 最佳实践
- React Hooks 最佳实践
- TypeScript 类型设计
- 代码重构原则

---

## 📝 更新日志

- **2025-01-XX**: 初始版本，包含高优先级模块清单和风险分析

---

## ⚠️ 重要提醒

1. **不要一次性提取所有模块**：按优先级逐步提取
2. **充分测试**：每个模块提取后都要充分测试
3. **保留原代码**：提取时先注释，确认无误后再删除
4. **文档更新**：提取后及时更新相关文档
5. **代码审查**：重要模块提取后进行代码审查

---

## 🔗 相关文档

- [OCRDetectionScreen 拆分备忘录](./OCRDetectionScreen_拆分备忘录.md)
- [代码重构最佳实践](./代码重构最佳实践.md)

