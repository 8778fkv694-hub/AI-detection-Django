# 整改记录

# 第一批整改记录：防漏单、串件和旧结果回写

整改日期：2026-09-09。对应审计报告 `docs/audit-2026-09-09/审计报告.md` 第 7 节"第一批"（A03/A04/A05/A06/A07/A11）。

## 整改内容

### A03 · 复位不能作废在途 OCR/LLM；旧结果和计时器仍可写回 → 已修复

- `src/hooks/ocr/useBatchResultHandler.ts`：每轮检测以唯一 `runId` 标识；`activeRunIdRef` 保存当前有效轮次，在 LLM await 之后、UI 写入前、保存前、保存回调后逐点校验；轮次被复位/取代时丢弃迟到结果（含融合 AI 本地状态复位 `resetFusionState`）。
- 自动放行的 2 秒延时器登记在 `autoNextTimerRef`，回调内先校验运行身份；`resetBatchSaveState` 升级为"作废在途轮次 + 清去重键 + 清计时器"。
- `src/hooks/ocr/useBatchProcessingManager.ts`：每次触发分配 `runId` 并传给结果回调。
- `src/screens/OCRDetectionScreen.tsx`：`inspectionRoundRef` 轮次身份；强制复位、确认继续均递增轮次，确认后的 1 秒清理延时器校验轮次，旧计时器不再把新流程设回 idle。

### A04 · 用识别内容去重，连续同款产品会漏记录 → 已修复

- 保存去重键由 `OCR文本_标签列表` 改为 `run:<runId>`（每次触发唯一）。同一轮内重复保存幂等（一次保存），不同件（新 runId）必然各存一条。验收：两件相同文字/标签产生 2 条保存。

### A05 · 保存失败不阻止进入下一轮 → 已修复

- `useDetectionSave.saveDetectionResult` 返回 `{saved, savedData, reason}`；保存失败（throw 或 `saved:false`）时批处理不自动放行：保持 `waiting_for_approval` + 等待确认，提示保存失败。
- 非融合模式下 OCR 失败（`success=false`）现在也保存"需复检"失败记录，不再"调用方视为保存完成却无记录"；无可保存结果时返回 `saved:false`。

### A06 · 视觉工装码跨件保留 → 已修复

- 后端 `product_trace_service._resolve_fixture_qr_from_candidates`：视觉来源绑定必须与当前图像候选交叉校验；当前图像存在唯一规则命中时以命中为准（新板换旧码 → 采用新码）；规则存在但旧码不符合规则时绑定判为可疑（`failed` → 存疑）；手工/扫码/NFC 显式绑定不受影响。
- 前端：自动继续、确认继续、强制复位时清除视觉识别的工装输入与上一件的追踪预览（`clearFixtureVisionBinding` / `prepareNextPiece`）。

### A07 · FQC 没有工装复用轮次 → 已修复（会话边界 + 兼容）

- 新模型 `FixtureLoadSession`（工装装载会话）；`InspectionResult.fixture_session` 外键；保存时由后端 `resolve_fixture_session` 解析/轮换会话。
- 会话轮换时机：前端声明 `fixtureNewRound`（产品循环进入下一件、强制复位；由 `traceContext.fixtureSessionId` 绑定纪元与 `fixtureNewRoundPending` 表达）且打开会话为空或已记录过同一工序（板子重新进入已完成工序 = 新件）时轮换；否则视为同一件的工序推进，沿用当前会话，避免把同件上游记录隔离出去。
- FQC `_collect_fixture_inspections`：触发记录携带会话时严格只收集同一会话记录；未携带会话（旧客户端/历史数据）保持旧行为（向后兼容）。FQC 生成后关闭会话。
- 已知边界（记录在案）：跳过首工序且上一轮未终检的快速复用，仍无法在无产品实例身份的情况下与"同件推进"区分（等价于审计 A07 修复方向中的 productInstanceId 装载生命周期，属第二批 A09 的产品实例持久化工作）；工装业务码比对（product_trace_service 跨工序业务码）仍可作为第二道防线。

### A11 · 后端结论未统一控制前端继续条件 → 已修复

- 保存回调读取后端权威结论（`trace_conclusion` / `fqc_overall_result` / `overall_quality`）：后端复核非合格时覆盖前端合格初判（`finalResult=unqualified`、报警灯已由 handleSaveComplete 按后端结论输出），并阻止自动放行（转人工确认）。前端初判在保存前显示，保存失败或后端拒绝不得当作最终合格。

## 验证记录

| 检查 | 结果 |
|---|---|
| `./node_modules/.bin/tsc --noEmit` | 通过 |
| `python3 manage.py test inspection.tests.test_ocr_fusion_safety inspection.tests.test_dual_window_detection --noinput`（backend 目录） | 21/21 通过 |
| `node docs/audit-2026-09-09/reproduce.cjs` | A03/A04/A05/A11 以"修复后行为"断言通过；A01/A10 仍按现状确认（第二批范围） |
| `python3 docs/audit-2026-09-09/reproduce_backend.py` | 6/6 通过（A07 会话边界 3 例、A06 候选校验 2 例、手工绑定 1 例；含 legacy 兼容与正常路径不受影响） |
| `python3 manage.py makemigrations --check` | 无遗漏迁移 |

`reproduce.cjs` / `reproduce_backend.py` 已按审计报告第 8 节要求改为预期正确行为的回归断言，并补充了向后兼容与正常路径不受影响的反向用例。

## 未尽事项（第二、三批）

- 第二批：A01（保存回配方字段一致性）、A02（工序切换原子性）、A08（就绪前置校验与资源准备）、A09（页面重启恢复协议、产品实例标识持久化——含 A07 残留边界的根治）。
- 第三批：A10（证据 schema：`ai_analysis.keyword_match_details`、`batch_processing.roi_details` 留存）、A12（模板版本化发布）、硬件故障注入与耐久验证。
- 本批未做：Jetson 部署、真实相机/串口/旋转机构验证；报告第 6 节验收矩阵的整体覆盖。

# 第二批整改记录：可信的模板一键导入（A01/A02/A08/A09）+ A10

整改日期：2026-09-09（同日续）。

## 整改内容

### A01 · 保存回配方遗漏字段，却把本地标成已同步 → 已修复

- `OCRDetectionScreen.tsx`：新增统一配方字段收集器 `currentRecipeState`（useMemo），差异检测与保存回配方使用**同一组字段、同一份数据**；`nonGridTargets`、`targetConfidences`、`fixtureEnabled`、`selectedStandardId`（以及此前漏比对的 `keywordMatchMode`、`minConfidence`、`detectionConfidence`、相机）全部纳入。
- 保存回配方以**服务端返回值**更新快照（`updateRecipe` 返回 `StageRecipe` → `setAppliedRecipe`），不再本地拼装快照。
- 归一化比较：忽略随机 UI id 与键序；`keywords` 字符串参与保存但不参与比对（以 `keywordConfigs` 为准，与原行为一致）；快照侧挑选同名字段，服务端元数据（name/updatedAt 等）不制造假差异。
- 验收路径（逐字段"编辑→保存→重新导入→执行"闭环）依赖服务端 PATCH 端点字段映射，`mapToApi` 已覆盖全部收集器字段。

### A02 · 工序切换先推进索引，加载失败后仍运行旧配方 → 已修复

- `useProductRecipe.ts`：`applyStage` 返回成功与否；`goToNextStage`/`goToPrevStage` **先加载校验应用，成功后才推进索引**与提示；失败保留原工序身份并明确提示"未切换成功，仍为原工序"。
- 选择自由模式或切换产品时作废在途工序请求（序号递增）；组件卸载同样作废。
- 产品加载恢复（含页面重启）时**重新校验并应用当前工序配方**，不再只恢复产品对象。

### A08 · "已应用配方"没有保证所需资源就绪 → 部分修复（可代码落地的部分）

- 开工前置检查：`checkRecipeReadiness` 按配方 `requiredDeviceTypes` 校验串口设备（sensor/controller/serial）连接状态；未就绪时明确提示并阻止开工（接入键盘手动抓拍与硬件触发两个开工入口），不静默跳过。
- 执行方式降级显式呈现：配方要求 LLM 融合而本页按 Jetson 算力策略强制关闭时，明确提示操作员（"如需启用请在当前窗口手动开启"）；强制关闭逻辑保留（Jetson 资源保护，不删除）。
- 未做（后续）：导入事务化（读模板→验证引用→准备相机模型设备→一次性提交有效配置→就绪）；指定相机无法解析时的就绪保证；模型失败回退的显式呈现——需接入设备/模型管理深水区。

### A09 · 页面重启后恢复半套配方，同时残留批次运行标志 → 部分修复

- `ocrDetectionStore.ts`：`roiCacheIds`（过期服务端 ROI 缓存 ID）与 `batchTriggered`（锁标志）**不再持久化**，刷新后必须重新采集上传、批处理重新开始。
- 受控配方身份（`appliedRecipeId/Name/Snapshot`）迁入 store 并持久化：刷新后设备动作映射、移动视角联控配置（`turntableEnabled` 等）可恢复，不再依赖组件内存丢失。
- `useProductRecipe` 恢复路径：页面重启后按持久化的产品与工序索引**重新校验并应用完整配方**（`applyStage`），不再只恢复产品对象。
- 未做：未结工件的"上次检测中断"标识与恢复策略（需保存状态协调，第三批持久化队列）。

### A10 · 保存裁剪掉规则与 ROI 证据 → 已修复（字段保留）

- `useDetectionSave.ts` 三个保存分支（融合/OCR 成功/OCR 失败）重新构造 `ocrResult` 时保留 `ai_analysis`（含 `keyword_match_details` 规则评估）与 `batch_processing`（含 `roi_details` 逐项匹配），历史可复核原判定依据。
- 版本化证据 schema、模板版本快照与大图留存策略属第三批。

## 验证记录（第二批）

| 检查 | 结果 |
|---|---|
| `./node_modules/.bin/tsc --noEmit` | 通过 |
| `python3 manage.py test inspection.tests.test_ocr_fusion_safety inspection.tests.test_dual_window_detection --noinput` | 21/21 通过 |
| `node docs/audit-2026-09-09/reproduce.cjs` | A01/A02/A08/A09/A10 均以修复后行为断言通过；A01/A02 各含失败路径反例 |
| `python3 docs/audit-2026-09-09/reproduce_backend.py` | 6/6 通过 |
| `python3 manage.py makemigrations --check` | 无遗漏 |

`goToNextStage/goToPrevStage` 由同步改为 async：所有调用方（下拉选择、上一/下一工序按钮）均为事件回调，返回值被忽略，兼容无影响。

## 整体状态

12 项发现：**10 项已整改**（A01-A05、A06、A11 全量；A08/A09/A10 为已落地的部分修复）。**A12（模板版本化发布与影响提示）尚未开始**；A08 的导入事务化、A09 的中断恢复协议、A10 的证据版本化、硬件故障注入与耐久验证属第三批后续工作。

# 第三批整改记录：配置治理与证据（A12/A10 补全）

整改日期：2026-09-09（同日续）。

## 整改内容

### A12 · 模板关联主要是复制，缺少版本化发布和影响提示 → 已落地（影响提示 + 语义显式化）

- 后端 `FixtureTemplateSerializer` 新增 `used_by_recipes`（哪些工序配方以快照复制引用了该工装模板）；`StageRecipeTemplateSerializer.to_representation` 拍平源模板当前版本（name/prefixes/pattern/updated_at）。
- `stageRecipeApi.ts`：`StageRecipe` 新增只读字段 `fixtureTemplateName/UpdatedAt`、`sourceFixturePrefixes/Pattern`。
- 运行页（`RecipeSelectModal`）：选中配方时若引用的工装模板已与配方快照分歧（前缀/正则不一致），显示明确警告"源工装模板已更新，本配方使用的是旧版本快照；改动不会自动同步"。
- 模板管理页（`TemplatesScreen`）：选择工装模板引用时固定提示"当前值是导入时的快照副本，后续修改模板不会自动同步，需要同步时重新选择"。
- 明确语义：引用 = 导入为独立副本（生产冻结快照）；源模板更新的影响经显式提示由配置人员决策，不存在"以为已联动"的静默路径。
- 未做：真正的"引用某一版本 + 校验发布新版本"完整发布流程（需要模板版本表与发布流，见整体状态）。

### A10 · 证据可追溯补全 → 已落地（配方身份）

- `OCRDetectionScreen.traceContext` 新增 `appliedRecipeId/appliedRecipeName/appliedRecipeUpdatedAt`，随保存进入 `trace_context`：每条检验记录可追溯"由哪个配方（版本）产生"，与 A07 的工装装载会话共同构成可复盘身份。

## 验证记录（第三批）

| 检查 | 结果 |
|---|---|
| `./node_modules/.bin/tsc --noEmit` | 通过 |
| `python3 manage.py test inspection.tests.test_ocr_fusion_safety inspection.tests.test_dual_window_detection --noinput` | 21/21 通过 |
| `node docs/audit-2026-09-09/reproduce.cjs` | 12 项发现中除硬件验证项外全部以修复后行为断言通过 |
| `python3 docs/audit-2026-09-09/reproduce_backend.py` | 6/6 通过 |
| `python3 manage.py makemigrations --check` | 无遗漏 |

## 整体状态（12 项发现）

| 发现 | 状态 |
|---|---|
| A01/A02/A03/A04/A05/A06/A07/A11 | ✅ 已整改 |
| A08 | 🔶 部分整改（就绪前置检查 + 降级呈现；导入事务化未做） |
| A09 | 🔶 部分整改（持久化边界 + 恢复重应用；未结工件中断协议未做） |
| A10 | 🔶 部分整改（证据字段保留 + 配方身份；版本化证据 schema/大图留存未做） |
| A12 | 🔶 部分整改（影响提示 + 语义显式化；版本化发布流程未做） |
| 验收矩阵/硬件故障注入/耐久验证 | ⬜ 未做（需 Jetson 实机） |
