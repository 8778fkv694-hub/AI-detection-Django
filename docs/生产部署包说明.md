# AI检测系统生产部署包说明

## 📦 部署包信息

- **文件名**: `wyl-ai-detection-production-8012.tar.gz`
- **大小**: 约1.1GB
- **版本**: v2.1.0
- **创建时间**: $(date '+%Y-%m-%d %H:%M:%S')

## 🚀 快速部署

### 1. 解压部署包
```bash
tar -xzf wyl-ai-detection-production-8012.tar.gz
cd wyl-ai-detection-production-8012
```

### 2. 配置环境变量
```bash
# 复制环境变量模板
cp production/env.example .env

# 编辑配置文件（重要：修改默认密码）
nano .env
```

### 3. 一键部署
```bash
# 给脚本执行权限
chmod +x deploy.sh stop_prod.sh check_prod_status.sh

# 执行部署
./deploy.sh
```

### 4. 验证部署
```bash
# 检查服务状态
./check_prod_status.sh

# 访问系统
# 前端: https://localhost
# 后端: http://localhost:8012/api/
# 管理: http://localhost:8012/admin
```

## 🔑 默认账户

- **管理员用户名**: admin
- **管理员密码**: admin123
- **重要**: 首次登录后请立即修改密码

## 📱 访问地址

### 生产环境
- **前端界面**: https://localhost
- **后端API**: http://localhost:8012/api/
- **管理后台**: http://localhost:8012/admin
- **健康检查**: http://localhost:8012/api/standards/

### 开发环境（参考）
- **前端界面**: http://localhost:3303
- **后端API**: http://localhost:8000/api/
- **管理后台**: http://localhost:8000/admin

## 🛠️ 服务管理

### 启动服务
```bash
docker-compose -f docker-compose.prod.yml up -d
```

### 停止服务
```bash
./stop_prod.sh
```

### 查看日志
```bash
# 查看所有服务日志
docker-compose -f docker-compose.prod.yml logs -f

# 查看特定服务日志
docker-compose -f docker-compose.prod.yml logs -f app
docker-compose -f docker-compose.prod.yml logs -f db
docker-compose -f docker-compose.prod.yml logs -f redis
```

### 重启服务
```bash
docker-compose -f docker-compose.prod.yml restart
```

## ⚙️ 配置说明

### 端口配置
- **HTTP**: 80
- **HTTPS**: 443
- **API**: 8012
- **数据库**: 5432
- **Redis**: 6379

### 数据存储
- **数据库数据**: Docker卷 `postgres_data`
- **媒体文件**: Docker卷 `app_media`
- **日志文件**: Docker卷 `app_logs`

## 🔒 安全建议

1. **修改默认密码**
   - 数据库密码
   - Redis密码
   - 管理员账户密码

2. **配置防火墙**
   - 只开放必要端口
   - 限制访问来源

3. **SSL证书**
   - 生产环境使用正式证书
   - 替换自签名证书

4. **定期备份**
   - 数据库备份
   - 媒体文件备份

## 🐛 故障排除

### 常见问题

1. **端口被占用**
   ```bash
   # 检查端口占用
   netstat -tulpn | grep :80
   netstat -tulpn | grep :8012
   ```

2. **权限问题**
   ```bash
   # 修复文件权限
   chmod +x deploy.sh
   chmod +x stop_prod.sh
   chmod +x check_prod_status.sh
   ```

3. **服务启动失败**
   ```bash
   # 查看详细日志
   docker-compose -f docker-compose.prod.yml logs app
   ```

### 重置系统
```bash
./stop_prod.sh
docker-compose -f docker-compose.prod.yml down -v
./deploy.sh
```

## 📊 系统要求

- **操作系统**: Linux (Ubuntu 20.04+ / CentOS 8+)
- **Docker**: 20.10+
- **Docker Compose**: 2.0+
- **内存**: 4GB+
- **磁盘**: 20GB+
- **CPU**: 2核心+

## 🔄 升级和维护

### 系统升级
```bash
# 拉取最新镜像
docker-compose -f docker-compose.prod.yml pull

# 重新构建
docker-compose -f docker-compose.prod.yml up -d --build
```

### 定期维护
- 清理Docker镜像
- 更新安全补丁
- 监控磁盘空间
- 备份重要数据

## 📞 技术支持

如遇到问题，请提供：
1. 系统环境信息
2. 错误日志
3. 部署步骤

联系开发团队获取技术支持。

---

## 📋 部署包内容

```
wyl-ai-detection-production-8012/
├── Dockerfile                    # Docker镜像构建文件
├── docker-compose.prod.yml      # 生产环境Docker编排
├── deploy.sh                    # 一键部署脚本
├── stop_prod.sh                 # 停止服务脚本
├── check_prod_status.sh         # 状态检查脚本
├── production/                  # 生产环境配置
│   ├── nginx.conf              # Nginx配置
│   ├── supervisord.conf        # 进程管理配置
│   ├── start.sh                # 容器启动脚本
│   ├── init-db.sql             # 数据库初始化
│   ├── env.example             # 环境变量模板
│   └── README.md               # 详细说明文档
├── backend/                     # Django后端代码
├── src/                        # React前端代码
├── package.json                # 前端依赖配置
├── vite.config.ts              # 前端构建配置
├── yolo10x.pt                  # AI模型文件
├── yolov8n.pt                  # AI模型文件
└── PPE_detection_YOLO/         # PPE检测模型
```

## 🎯 部署流程

1. **环境准备** → 安装Docker和Docker Compose
2. **解压部署包** → 解压到目标目录
3. **配置环境** → 修改.env配置文件
4. **执行部署** → 运行deploy.sh脚本
5. **验证部署** → 检查服务状态和访问地址
6. **安全配置** → 修改默认密码和证书
7. **开始使用** → 访问系统进行测试

部署完成后，您将拥有一个完整的AI检测系统，支持PPE检测、洁净用品检测等功能。
