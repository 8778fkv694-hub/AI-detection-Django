# 后期优化方向 Hand-Off

**日期**：2026-07-16（修订版，关键断言已逐条对照源码复核）
**定位**：改善周的正确性修复已完成（见《改善方案_近两日代码审查.md》），本文档定义下一阶段目标：把系统从"能跑通"推到"真正的生产力"。三条业务主线**并行推进**：

- **A 线：多窗口并行检验生产化**（当前最迫切的业务需求）
- **B 线：抓拍清晰度**（卡顿/运动场景抓不到最清晰帧）
- **C 线：复检快速闭环**（存疑/需复检后定点复检，不整机重来）

外加一条 **D 线：工程秩序**，保证前三条能稳定叠加。

> 工作量估算均按单人开发计。凡标注"⚠️ 先测量"的项，动手前先拿数据，避免优化不存在的问题。

---

## 现状底座（已具备的能力，均已在源码确认）

| 能力 | 位置 | 状态 |
|---|---|---|
| 双窗口检测循环隔离（owner_id 路由）、锁竞争已修复 | `backend/inspection/detection_loop.py` | ✅ |
| 模型固定（pin）+ 单 GPU 公平推理调度 | `backend/inspection/yolo.py` | ✅ |
| ROI 缓存 owner 作用域 + TTL | `backend/inspection/roi_cache.py` | ✅ |
| 后端 30 帧原始分辨率环形缓冲，按 frame_id 取帧 | `detection_loop.py` `_frame_buffer` / `get_snapshot_jpeg` | ✅ |
| **抓拍帧对齐已存在**：前端触发抓拍时用 frame_id 向后端环形缓冲取对齐帧，失败才退化 canvas | `useRealtimeDetectionLoop.ts:586-627` `fetchStreamSnapshot` | ✅ 注意：不是缺口 |
| **OCR 引擎级互斥锁已存在**：并发批处理按 ROI 粒度交错，不会并行跑双 PaddleOCR | `ocr_service.py` `_inference_locks` | ✅ 注意：不是缺口 |
| 批处理逐 ROI 处理 + 逐规则结论 | `batch_detection_service.py` `_process_single_roi` / `barcode_match_details` | ✅ 尚未被复检链路利用 |
| 记录含逐 ROI 明细 + OCR 兜底标注 | `useDetectionSave.ts`、`barcode_analysis.results[].source` | ✅ 本周已加 |
| 追溯结论可被后到的合格结果修复 | `product_trace_service`（test_quality_recovers_after_matching_stage_arrives） | ✅ |

---

## A 线：多窗口并行检验生产化

双窗口隔离本周已"能用"（owner 路由、锁竞争、缓存作用域都修了）。到"生产力"还缺的按重要性排列：

**A1 窗口崩溃回收（stale owner 收割）— 最先做，小工作量**
窗口异常关闭（断电、浏览器崩溃）不会发 stop，其检测循环和 pinned 模型永远挂着；Jetson 模型池只有 2 个名额，挂两次别的窗口就再也启动不了，只能重启服务。已确认代码中没有任何 owner 过期机制。
方案：owner 心跳——前端本来就在高频轮询 `get_latest_result`（带 owner_id），后端顺手刷新该 owner 的 `last_seen`；管理器在每次 `start_loop`/定时器里收割超 60s 无心跳的 owner（复用现有 `_detach_owner_locked`）。补一个"崩溃窗口 90 秒内资源回收"的测试。

**A2 会话概念沉淀（中；已确认规模=同机 2 窗口，不带设备维度）**
owner_id、scoped storage key、roi_cache owner、追溯 pageInstanceId 现在是四套各自为政的标识（审查 M-4 的重复代码就是症状）。定义统一的 `WorkstationSession`（会话 id + 工位 + 配方 + 模型），前端一处生成、所有 API 统一携带。业务已确认目标就是单机双窗口——**不设计设备维度**，不做多 Jetson 集中看板，复杂度相应下调。注意 Android APK（`android-app/`、`AppMobile.tsx`）走同一批 API，会话字段设计要把移动端算进去。

**A3 并发批处理的体验保障（中，⚠️ 先测量）**
已确认 `ocr_service` 有引擎级互斥锁：两个窗口同时批处理时按 ROI 粒度交错执行，不会内存爆炸，但**双方总时长都近似翻倍且无任何提示**。先在双窗口下实测批处理耗时分布，再决定做哪个：
- 轻方案：批处理进度/排队状态透出（前端显示"另一工位处理中，预计还需 Ns"）；
- 重方案：OCR 调度加优先级——交互式单 ROI 复检（C 线 R2）优先于整批任务插队执行。
不建议并行双 OCR 实例——Jetson 8GB 扛不住，交错已是合理策略。

**A4 容量边界显式化（前端，小）**
模型池上限 2 是物理约束。第三个窗口加载新模型时现在只返回报错文本；应给容量面板（"当前 2/2：窗口A=PPE，窗口B=OCR"）和可操作建议（复用已加载模型 / 关闭某窗口）。后端 `get_model_pool_status` 已含 pinned 与调度器状态，纯前端工作。

**A5 双窗口验收测试（中）**
现有 `test_dual_window_detection` 覆盖管理器逻辑，缺端到端场景：同流不同模型、同模型不同流、一窗批处理中另一窗启停循环、崩溃回收、两窗口同时置信度调整。固化为 CI 必跑项，多窗口的回归才有防线。

**验收标准**：两窗口各自连续检测 1 小时无相互干扰；任一窗口崩溃后 90 秒内资源自动回收；并发批处理时双方都能看到进度预期。

---

## B 线：抓拍清晰度

### 现状（已核对源码，与直觉不同）
帧对齐问题**已经解决**：抓拍触发时前端用检测结果的 frame_id 向后端环形缓冲取原始帧（`fetchStreamSnapshot`），预览卡顿不影响这条路径。因此"卡顿抓不到清晰照片"的真实原因大概率是以下三个之一，**必须先测量再动手**：

1. **触发瞬间的运动模糊**——对齐取到的就是触发那一帧，工件在动、曝光偏长，那帧本身就是糊的（最可能）；
2. **退化路径被走到**——后端快照失败时 fallback 到 canvas 截预览图（`useRealtimeDetectionLoop.ts:614`），或某些分支 `dataUrl` 已被 canvas 预填充而跳过服务器取帧；退化时就是滞后帧+二次压缩；
3. **ROI 二次压缩**——批处理的 ROI 由前端从抓拍帧裁剪后再编码 JPEG 上传（`useBatchProcessingManager.ts:74`），比服务器直裁多一代有损。

### 分解任务

**B0 抓拍链路埋点（最先做，小，⚠️ 这是后续一切的依据）**
每次抓拍在记录 metadata 里写入：图像来源（`server_frame / canvas_fallback / preset_dataurl`）、请求 frame_id 与实际命中 frame_id、清晰度分数（Laplacian 方差，后端一行 OpenCV）。跑几天产线，看清楚糊片到底来自哪条路径、分数分布如何。**没有这个数据，B2/B3/B4 都是猜。**

**B1 环形缓冲内选最清晰帧（小）**
`get_snapshot_jpeg` 加 `select='sharpest'` 参数：在请求 frame_id ±N 邻域内按清晰度评分选最优帧返回（响应头带回实际 frame_id 与分数，前端已有 X-Frame-ID 处理逻辑可复用）。触发帧糊、邻近帧清晰的场景直接受益。

**B2 抓拍时机门控（中，依赖 B0 数据；已定为按工位可配置开关）**
现在防抖只看"检测框连续存在"。改为：框位置稳定 K 帧（IoU > 阈值）**且**清晰度分数超下限才触发；不满足时 UI 提示"画面移动中，请稳定工件"。运动模糊的根本解法——糊帧再怎么选也是糊的。业务已确认**两种节拍都存在**：门控做成工序配方里的开关（静止工位默认开，不停线工位关），配置项进 `stageRecipeApi` 现有配方体系。

**B3 ROI 裁剪搬服务器侧（中，收益=省一代 JPEG + 省带宽往返）**
前端批处理时只传 `bbox + frame_id + owner_id`，后端从环形缓冲取**原始 numpy 帧**直裁入 ROI 缓存（`roi_cache_api` 加 `source='server_frame'` 分支）。保留 base64 路径给本地离线模式（浏览器端 ONNX 检测时后端没有环形缓冲）。注意环形缓冲仅 30 帧（约 2 秒），批处理触发紧跟检测，正常命中；未命中时按现有语义退化到最新帧并在 metadata 标注。

**B4 采集参数与硬件（对不停线工位为必须项，参数以 B0 数据定）**
业务已确认存在工件持续运动、等不起门控的工位——对这类工位，运动模糊只能从采集端解决：v4l2 锁定曝光/增益、短快门+补光，必要时独立全分辨率 still-capture 通道。静止工位仍以 B2 软件门控为主。**具体快门/补光参数等 B0 埋点数据出来再定，不要盲买。**

**验收标准**：连续 50 次抓拍清晰度分数 P10 不低于人工静止拍摄基线的 80%；`canvas_fallback` 占比 < 1%；人为限速代理模拟卡顿时抓拍质量不下降。

---

## C 线：复检快速闭环

### 问题
存疑/需复检后只能整机重测，而记录里已经知道**具体哪个 ROI 的哪条规则失败**——重来一遍等于把已知信息扔掉。复检目前在结果页只是一个状态徽章（`OCRInspectionResultsScreen` 等），没有任何操作入口。

### 目标流程
```
结果页失败项（"serial_label：一维条码规则未匹配"）
  → 点【复检此项】
  → 单目标复检模式：取景框只引导对准这一个目标 + 实时清晰度提示（复用 B1 分数）
  → 后端只重跑该 ROI 的规则子集（秒级）
  → 通过 → 结论修复，复检链留痕；不通过 → 重试或转人工判定
```

### 分解任务

**R1 数据模型（后端，小）**
`InspectionResult` 增加 `parent_record_id`（复检指向原记录）、`recheck_scope`（覆盖的 ROI label 列表）、`resolution`（`auto_pass / recheck_pass / manual_override / stay_failed`）。原记录**永不覆盖**——复检生成新记录并回写原记录的最终结论字段，保留完整证据链（"需复检→复检合格"双状态，见开放问题 1）。

**R2 复检 API（后端，中）⚠️ 必须与 D1 规则统一绑定**
`POST /api/records/{id}/recheck/`，body 带 `roi_labels` + 新抓拍（bbox+frame_id，直接走 B3 的服务器侧取帧）。内部把 `_process_single_roi` + `_validate_roi` 封装成公开 service 函数后调用（不要直接调私有方法），只跑目标 ROI 的规则子集，与原记录其余 ROI 结论合并出新总结论。**不要**重新走完整 `process_batch`。若 A3 做了优先级，复检任务应插队于整批任务之前。

**R3 前端复检入口（前端，中）**
结果页失败 ROI 行内加【复检此项】；检测页支持单目标模式（现有 `selected_targets` 参数传单个 label 已支持）。完成后记录状态原位刷新。移动端（`AppMobile`）同步适配布局。

**R4 人工判定通道（前端+后端，小；已定不分权限）**
机器复检解决不了的失败（印刷本身缺陷、目标确实缺失）走【人工判定】：合格/不合格 + 必填理由 + 操作人，写 `resolution='manual_override'`，reason 追加标注（与本周 OCR 兜底标注同一模式同一展示位）。业务已确认**不做账号/角色体系**，留痕即可。

**R5 追溯联动（后端，小）**
复检通过后工位追溯结论同步修复。`product_trace_service` 已有"后到合格结果修复结论"机制，复检记录走同一入口，补一个回归测试。

**验收标准**：从"看到失败"到"复检出结论"≤ 30 秒；复检记录与原记录双向可查；人工判定必留操作人与理由。

---

## D 线：工程秩序

1. **D1 规则引擎统一（审查 M-1，与 R2 强制绑定）**：条码/关键词规则评估目前后端 + 前端两份半实现。R2 落地时前端改为直接消费后端 `barcode_match_details`，消灭漂移源。**做 R2 不做 D1，复检结论就是第三份实现，和检测结论早晚打架。**
2. **CI 最小闭环（半天，建议所有事之前先做）**：`tsc --noEmit` + `python manage.py test inspection`（38 个）+ `scripts/check-bundle-budget.mjs` 都已存在，只差一个 workflow 串起来。A5/B/C 的每一步都需要这道防线。
3. **可观测性（小）**：聚合 `/api/health/`（流 FPS / 各循环推理 FPS / 模型池 / OCR 锁等待 / 内存水位）+ 前端状态条。`get_model_pool_status`、roi-cache-stats 已存在，是拼装不是新建。多窗口出问题时判断"卡在哪"的第一手段，也是 A3"先测量"的载体。
4. **遗留清理**：审查文档 E-2（无规则 ROI 白跑 OCR，直接影响批处理耗时，随 R2 一起做）、M-2/M-3/M-5/M-6（低风险重构，穿插进行）、M-7（评估页假随机指标，删或真算）。
5. **P1-4 兜底阈值跟踪**：已按需求保留 + 记录标注。上线后按月抽检 `reasonKeywords` 含 `OCR兜底` 的记录，用数据决定是否收紧到 8 位。
6. **记录存储治理（小，别拖太久；策略已定）**：检测记录随图片 base64 入库，产线连续运行会持续膨胀。已定策略：**原图保留 30 天，检测结论与缩略图永久**。实现为定时清理任务（可挂在现有 Django 管理命令 + cron），删除前生成缩略图回写记录。

---

## 建议里程碑

| 阶段 | 内容 | 产出 |
|---|---|---|
| **M1（第 1~2 周）** | D2 CI → A1 崩溃回收 → B0 埋点 → R1/R2+D1 复检后端 | 多窗口不再被崩溃锁死；抓拍问题有数据；复检 API 可调 |
| **M2（第 3~4 周）** | R3/R4/R5 复检前端闭环 → B1 选帧 → A3 测量+轻方案 → A5 验收测试 | 操作员 30 秒定点复检；抓拍质量第一轮提升；双窗口有回归防线 |
| **M3（持续）** | B2 门控 → A2 会话沉淀 → A4 容量面板 → 可观测性 → 清理项 → B4（按 B0 数据） | 系统"有序"：状态可见、行为可预期、代码单一事实源 |

排序理由：A1 和 B0 都是小工作量高杠杆，先做；复检闭环（C 线）是操作员感知最强的单项能力，横跨 M1/M2；B2/B4 依赖 B0 的数据，不提前。

## 业务决策（2026-07-16 已由需求方拍板，不再是开放问题）

| # | 问题 | 决策 | 对任务的影响 |
|---|---|---|---|
| 1 | 复检后原记录状态 | **双状态留痕**（"需复检→复检合格"） | R1 按 `resolution` 字段 + 原记录不覆盖实现，结果页展示两段状态 |
| 2 | 人工判定权限 | **不分权限，留痕即可**（必填理由 + 操作人） | R4 不建账号/角色体系，工作量保持"小" |
| 3 | 节拍是否允许"等稳定再拍" | **两种场景都有** | B2 门控做成**按工位配置的开关**（静止工位开门控；不停线工位关门控）；对不停线工位，B4 补光+短快门从"可选"升级为**必须项**，仍以 B0 数据定参数 |
| 4 | 多窗口规模 | **同机 2 窗口即可**，不做多设备看板 | A2 会话设计**不带设备维度**，复杂度下调；A4 容量面板按单机双工位设计 |
| 5 | 记录原图保留 | **原图 30 天，结论永久** | D6 实现：定时任务 30 天后删原图、保留缩略图+完整检测结论；Jetson 磁盘压力小，无需导出归档工具 |

---

## 本次修订记录（相对初版）

- **纠错**：初版称"抓拍滞后帧是根因、C1 服务器取帧可消除"——核对代码发现 frame_id 对齐取帧**已存在**（`fetchStreamSnapshot`），真实缺口是清晰度选帧/门控与退化路径，B 线全部重写并前置埋点（B0）。
- **纠错**：初版称"两窗口批处理时第二个要等第一个整批跑完"——实际 `ocr_service` 已有引擎级互斥锁，按 ROI 交错执行；A3 改为"先测量，再决定做进度透出还是优先级调度"。
- **调整**：里程碑从"复检优先"改为三线并行、按依赖与杠杆排序（A1/B0 先行），与"多窗口是当前最迫切需求"的业务判断对齐。
- **补充**：移动端/APK 适配提醒（A2/R3）、记录存储治理（D6）、复检任务插队与 A3 的联动、估算按单人开发口径。
