# 优化修改记录 (2025-12-28)

本次修改主要包含：P0安全修复、P1-P2优化、分页兼容性修复、数据库连接优化、OCR性能优化。

---

## ✅ 已完成优化 (2025-12-28 18:00-19:00)

### 1. OCR方向检测动态开关 ✅
**提交**: `6e37e95` - feat: 实现PaddleOCR方向检测动态开关

**修改文件**:
- `backend/inspection/ocr_service.py` - 双实例模式支持动态方向检测
- `backend/inspection/ocr_api.py` - 接收 use_angle_cls 参数
- `src/hooks/ocr/useOCRProcessing.ts` - 根据关键词配置自动决定
- `src/screens/OCRDetectionScreen.tsx` - 修复二维码检测状态同步

**优化效果**:
- 当所有关键词设为"不检测方向"时，自动关闭方向检测（性能提升约30%）
- 双实例模式避免重复加载模型，支持动态切换
- 修复 zustand persist hydration 导致的二维码检测初始化问题

### 2. 日志管理优化 ✅
**提交**: `77e6808` - refactor: 优化日志管理减少日志量

**修改文件**:
- `backend/config/settings.py` - 添加日志轮转配置
- `backend/inspection/middleware.py` - 日志限频 + 级别调整
- `backend/inspection/yolo.py` - print → logging（8处）

**优化效果**:
- 日志文件自动轮转（10MB/文件，保留5个备份，总量不超过50MB）
- 数据库重连日志每60秒最多1次（原来每次重连都记录，刷屏严重）
- 同步相关日志降为DEBUG级别，减少INFO日志输出
- 规范化日志输出，便于生产环境调试

---

## 📋 待实施优化

## 一、P0 安全修复

### 1. backend/config/settings.py
```python
# DEBUG 默认改为 False
DEBUG = os.environ.get('DEBUG', 'False') == 'True'

# ALLOWED_HOSTS 从环境变量读取
ALLOWED_HOSTS = os.environ.get('ALLOWED_HOSTS', 'localhost,127.0.0.1').split(',')

# CORS 只在 DEBUG 模式下允许所有来源
CORS_ALLOW_ALL_ORIGINS = DEBUG

# 添加分页配置
REST_FRAMEWORK = {
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
    'PAGE_SIZE': 100,
    'EXCEPTION_HANDLER': 'inspection.exception_handler.custom_exception_handler',
}
```

### 2. 新增 backend/inspection/exception_handler.py
```python
# 生产环境隐藏错误详情
def custom_exception_handler(exc, context):
    if settings.DEBUG:
        return Response({'error': str(exc), ...})
    else:
        return Response({'error': '服务器内部错误，请稍后重试', 'code': 'INTERNAL_ERROR'}, ...)
```

---

## 二、P1-P2 优化

### 1. ✅ 日志轮转配置 (backend/config/settings.py) - 已完成
```python
LOGGING = {
    'handlers': {
        'file': {
            'class': 'logging.handlers.RotatingFileHandler',
            'maxBytes': 10 * 1024 * 1024,  # 10MB
            'backupCount': 5,
        }
    }
}
```

### 2. ✅ backend/inspection/yolo.py - 已完成
- 将 `print()` 替换为 `logging` （8处已全部替换）
- 修复裸 `except:` 为具体异常类型（待优化）

### 3. backend/inspection/models.py
```python
# 添加数据库索引
timestamp = models.DateTimeField(auto_now_add=True, db_index=True)
overall_quality = models.CharField(..., db_index=True)
detection_type = models.CharField(..., db_index=True)

class Meta:
    indexes = [
        models.Index(fields=['detection_type', 'timestamp']),
        models.Index(fields=['overall_quality', 'timestamp']),
        models.Index(fields=['-timestamp']),
    ]
```

### 4. backend/Dockerfile
```dockerfile
# 添加非root用户
RUN groupadd --gid 1000 appgroup \
    && useradd --uid 1000 --gid appgroup --shell /bin/bash --create-home appuser
COPY --chown=appuser:appgroup . .
USER appuser
```

---

## 三、分页兼容性修复

后端添加分页后，前端需要处理 `{count, results, ...}` 格式的响应。

### 修改的文件：
1. **src/state/appStore.ts** - fetchStandards, fetchResults
2. **src/api/streamApi.ts** - getStreamSources, getActiveStreams
3. **src/hooks/ocr/useOCRHistory.ts** - loadHistory
4. **src/lib/ocrTemplatesApi.ts** - fetchKeywordTemplates, fetchBarcodeTemplates

### 统一修改模式：
```typescript
const data = await response.json();
const resultsArray = Array.isArray(data) ? data : (data.results || []);
```

---

## 四、✅ 数据库连接稳定性优化 - 已完成

### backend/inspection/middleware.py

#### ✅ DataConsistencyMiddleware - 已完成
```python
# 添加类级别计数器，限制日志频率
_reconnect_count = 0
_last_reconnect_log = 0

def process_request(self, request):
    try:
        connection.ensure_connection()
    except Exception:
        current_time = time.time()
        DataConsistencyMiddleware._reconnect_count += 1
        # 每60秒最多记录一次警告
        if current_time - DataConsistencyMiddleware._last_reconnect_log > 60:
            logger.warning(f'数据库连接重建 (最近60秒重连次数: {DataConsistencyMiddleware._reconnect_count})')
            DataConsistencyMiddleware._reconnect_count = 0
            DataConsistencyMiddleware._last_reconnect_log = current_time
        connection.close()
        connection.ensure_connection()
```

#### ✅ AutoSyncMiddleware - 已完成
- ✅ 日志级别从 INFO 改为 DEBUG（已完成）
- ⏸️ 移除 `cache.clear()` 调用（待评估，可能影响数据同步）

---

## 五、OCR性能优化

### 1. backend/inspection/ocr_service.py

#### ⏸️ 添加性能计时日志（待实施）
```python
def extract_text(self, image_data, model_name=None, use_angle_cls=False):
    import time
    total_start = time.time()
    # ...
    decode_time = time.time() - decode_start
    ocr_time = time.time() - ocr_start
    logger.info(f"⏱️ OCR性能: 图片解码={decode_time:.2f}s, OCR推理={ocr_time:.2f}s, 总计={total_time:.2f}s")
```

#### ❌ M2 Mac 优化配置（已放弃 - PaddleOCR参数不兼容）
```python
os.environ['OMP_NUM_THREADS'] = '6'
os.environ['MKL_NUM_THREADS'] = '6'

self.paddleocr_reader = paddleocr.PaddleOCR(
    use_angle_cls=True,  # 加载模型，运行时通过参数控制
    lang='ch',
    use_gpu=False,
    enable_mkldnn=False,
    show_log=False,
)
```

**注意：以下参数在当前PaddleOCR版本不支持，已移除：**
- `det_limit_side_len`
- `rec_batch_num`
- `cpu_threads`
- `use_mp`
- `det_db_score_mode`

#### ✅ 动态角度分析控制 - 已完成（双实例模式）
```python
# 使用双实例模式，根据 use_angle_cls 加载对应实例
def _load_paddleocr_model(self, use_angle_cls=False):
    if use_angle_cls:
        return self.paddleocr_reader_with_cls  # 开启方向检测
    else:
        return self.paddleocr_reader_without_cls  # 关闭方向检测

def extract_text(self, image_data, model_name=None, use_angle_cls=False):
    paddleocr_reader = self._load_paddleocr_model(use_angle_cls=use_angle_cls)
    results = paddleocr_reader.ocr(image_array)
```

### 2. ✅ backend/inspection/ocr_api.py - 已完成
```python
use_angle_cls = data.get('use_angle_cls', False)  # 默认关闭
result = ocr_service.extract_text(image_b64, model_name, use_angle_cls=use_angle_cls)
```

### 3. ⏸️ backend/inspection/wechat_qr_service.py（待实施）
添加性能计时日志：
```python
timing_stats = {'decode': 0, 'detect': 0, 'encode': 0}
# ...
logger.info(f"⏱️ 二维码性能: 解码={timing_stats['decode']:.2f}s, 检测={timing_stats['detect']:.2f}s, ...")
```

### 4. ✅ src/hooks/ocr/useOCRProcessing.ts - 已完成（自动决策模式）
```typescript
// 根据关键词配置自动决定是否需要方向检测
const needsAngleDetection = keywordConfigs.some(
  config => config.expectedOrientation !== undefined && config.expectedOrientation !== null
);
console.log(`🧭 方向检测: ${needsAngleDetection ? '开启' : '关闭'} (根据关键词配置自动决定)`);

// 传递到API
body: JSON.stringify({
  image: base64Data,
  model: selectedModel,
  use_angle_cls: needsAngleDetection,
})
```

### 5. ❌ src/state/ocrDetectionStore.ts - 不需要（采用自动决策）
~~原计划手动切换，改为自动决策~~

### 6. ✅ src/screens/OCRDetectionScreen.tsx - 已完成
- ✅ 二维码检测自动启用（修复 hydration 问题）
- ❌ 手动方向检测开关（改为自动决策）

---

## 六、前端配置优化

### src/lib/config.ts
```typescript
// API端口可配置
const API_HOST = import.meta.env.VITE_API_HOST || window.location.hostname;
const API_PORT = import.meta.env.VITE_API_PORT || '8012';
```

---

## 已知问题

1. **PaddleOCR参数兼容性**：当前安装的PaddleOCR版本不支持以下参数：
   - `det_limit_side_len`
   - `rec_batch_num`
   - `cpu_threads`
   - `use_mp`
   - `det_db_score_mode`

2. **OCR性能**：在M2 Mac上，PaddleOCR推理约需15秒，主要因为：
   - 无GPU加速（M2不支持CUDA）
   - 仅使用CPU运算

---

## 文件变更列表

```
backend/config/settings.py
backend/inspection/exception_handler.py (新增)
backend/inspection/middleware.py
backend/inspection/models.py
backend/inspection/ocr_api.py
backend/inspection/ocr_service.py
backend/inspection/views.py
backend/inspection/wechat_qr_service.py
backend/inspection/yolo.py
backend/Dockerfile
src/api/streamApi.ts
src/hooks/ocr/useOCRHistory.ts
src/hooks/ocr/useOCRProcessing.ts
src/lib/config.ts
src/lib/ocrTemplatesApi.ts
src/screens/OCRDetectionScreen.tsx
src/state/appStore.ts
src/state/ocrDetectionStore.ts
.gitignore
```
