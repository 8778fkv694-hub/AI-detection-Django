# 批处理功能集成示例

## 📦 已创建的文件

### 后端
1. ✅ `backend/inspection/roi_cache_api.py` - ROI缓存API
2. ✅ `backend/config/urls.py` - 添加了 `/api/yolo/cache-roi/` 路由

### 前端
1. ✅ `src/hooks/ocr/useBatchProcessingManager.ts` - 批处理管理器Hook
2. ✅ `src/components/ocr/BatchProcessingPanel.tsx` - 批处理状态面板组件

---

## 🔧 在OCRDetectionScreen中集成

### Step 1: 导入新的Hook和组件

在 `src/screens/OCRDetectionScreen.tsx` 的顶部添加导入：

```typescript
// ... 现有imports ...

import { useBatchProcessingManager } from '@/hooks/ocr/useBatchProcessingManager';
import { BatchProcessingPanel } from '@/components/ocr/BatchProcessingPanel';
```

### Step 2: 在组件中使用批处理管理器

在 `OCRDetectionScreen` 组件内部，添加批处理管理器Hook：

```typescript
const OCRDetectionScreen: React.FC = () => {
  // ... 现有代码 ...
  
  // 批处理管理器
  const batchManager = useBatchProcessingManager({
    selectedTargets,
    enable KeywordAnalysis,
    keywordConfigs,
    enableBarcodeDetection,
    
    // 批处理完成回调
    onBatchComplete: (result) => {
      console.log('✅ 批处理完成，更新UI:', result);
      
      // 更新OCR结果显示
      setOcrResult({
        success: true,
        fullText: result.ocr_text,
        detailedResults: result.details.map(d => ({
          text: d.ocr_text,
          confidence: 0,
          bbox: d.bbox || []
        })),
        matchStatus: result.overall_quality === '合格' ? 'qualified' : 'unqualified',
        overallQuality: result.overall_quality,
        reason: result.reason
      });
      
      // 显示拼接图
      if (result.stitched_image) {
        setImagePreview(result.stitched_image);
      }
      
      // 更新工作流状态
      setWorkflowState('completed');
      setFinalResult(result.overall_quality === '合格' ? 'qualified' : 'unqualified');
      
      // 2秒后重置批处理状态
      setTimeout(() => {
        batchManager.reset();
      }, 2000);
    }
  });
  
  // ... 其余代码 ...
}
```

### Step 3: 在实时检测中缓存ROI

找到实时检测处理ROI的位置（大约在 `performRealtimeDetection` 函数或类似位置），添加ROI缓存逻辑：

```typescript
// 在检测到ROI后，缓存到后端
const handleDetectedROI = async (label: string, roiImageDataUrl: string, bbox: any, detection: any) => {
  // 如果是批处理模式，缓存ROI到后端
  if (batchManager.status.mode === 'batch') {
    await batchManager.cacheROI(label, roiImageDataUrl, bbox, detection);
    
    console.log(`📦 ROI已缓存到后端: ${label}`);
  }
  
  // ... 现有的ROI处理逻辑 ...
};
```

**具体集成位置建议**：

查找类似这样的代码（ROI提取逻辑）：

```typescript
// 现有代码示例（需要查找实际位置）
for (const detection of validDetections) {
  const label = detection.label;
  const bbox = detection.bbox;
  
  // 提取ROI图像
  const roiImageDataUrl = extractROIImage(frameDataUrl, bbox);
  
  // 计算清晰度等...
  
  // 👇 在这里添加批处理缓存
  if (batchManager.status.mode === 'batch') {
    batchManager.cacheROI(label, roiImageDataUrl, bbox, detection);
  }
}
```

### Step 4: 添加UI显示

在设置面板或检测状态区域添加批处理面板：

```typescript
{/* 在适当的位置（例如设置面板中）添加批处理面板 */}
<BatchProcessingPanel status={batchManager.status} />
```

**推荐位置**：
- 在"ROI截图和YOLO检测增强设置"附近
- 或者在"实时检测设置"附近
- 或者作为独立的设置卡片

### Step 5: 测试集成

1. **启动后端**:
   ```bash
   cd backend
   python manage.py runserver
   ```

2. **前端测试步骤**:
   - 在设置中选择"批处理模式"
   - 启动实时检测
   - 观察console日志中的 `📦 ROI已缓存到后端` 消息
   - 等待所有目标检测完成
   - 观察 `🚀 触发批处理检测` 和 `✅ 批处理完成` 消息
   - 检查UI显示的结果

---

## 📝 完整代码示例（简化版）

```typescript
// src/screens/OCRDetectionScreen.tsx

import React from 'react';
import { useBatchProcessingManager } from '@/hooks/ocr/useBatchProcessingManager';
import { BatchProcessingPanel } from '@/components/ocr/BatchProcessingPanel';
// ... 其他imports ...

const OCRDetectionScreen: React.FC = () => {
  // ... 现有state和hooks ...
  
  // 批处理管理器
  const batchManager = useBatchProcessingManager({
    selectedTargets,
    enableKeywordAnalysis,
    keywordConfigs,
    enableBarcodeDetection,
    onBatchComplete: handleBatchComplete,
  });
  
  // 批处理完成回调
  const handleBatchComplete = useCallback((result) => {
    // 更新OCR结果
    setOcrResult({
      success: true,
      fullText: result.ocr_text,
      // ... 其他字段
    });
    
    // 更新拼接图
    if (result.stitched_image) {
      setImagePreview(result.stitched_image);
    }
    
    // 更新工作流
    setWorkflowState('completed');
    setFinalResult(result.overall_quality === '合格' ? 'qualified' : 'unqualified');
    
    // 延迟重置
    setTimeout(() => batchManager.reset(), 2000);
  }, [batchManager, setOcrResult, setImagePreview, setWorkflowState, setFinalResult]);
  
  // 在实时检测中缓存ROI（在适当位置调用）
  const handleROIDetected = useCallback(async (label, roiDataUrl, bbox, detection) => {
    if (batchManager.status.mode === 'batch') {
      await batchManager.cacheROI(label, roiDataUrl, bbox, detection);
    }
    // ... 现有逻辑 ...
  }, [batchManager]);
  
  return (
    <div>
      {/* ... 现有UI ... */}
      
      {/* 批处理状态面板 */}
      <BatchProcessingPanel status={batchManager.status} />
      
      {/* ... 其他UI ... */}
    </div>
  );
};
```

---

## 🎯 关键集成点

### 1. ROI缓存时机
在实时检测循环中，每次检测到新的ROI时调用 `batchManager.cacheROI()`

### 2. 自动触发
批处理管理器会自动检测所有目标是否齐全，齐全后自动触发批处理（在useEffect中实现）

### 3. 结果处理
通过 `onBatchComplete` 回调接收批处理结果，更新UI显示

### 4. 状态重置
批处理完成后，记得调用 `batchManager.reset()` 清理状态，为下一次检测做准备

---

## ⚠️ 注意事项

1. **模式判断**: 只在 `batchProcessingMode === 'batch'` 时缓存ROI和触发批处理
2. **防止重复**: 批处理管理器内部已实现防重复触发机制
3. **错误处理**: 缓存ROI失败不会阻塞现有流程，只会console.error
4. **状态清理**: 批处理完成后2秒自动重置状态
5. **内存管理**: ROI缓存默认5分钟过期，自动清理

---

## 🧪 调试建议

### Console日志关键字

- `📦 ROI已缓存到后端` - ROI缓存成功
- `🎯 所有目标就绪` - 所有目标检测完成
- `🚀 触发批处理检测` - 开始批处理
- `✅ 批处理完成` - 批处理成功
- `❌ 批处理失败` - 批处理失败
- `🔄 批处理状态已重置` - 状态重置

### 网络请求检查

1. 打开浏览器DevTools → Network
2. 筛选 XHR/Fetch
3. 查看以下请求：
   - `POST /api/yolo/cache-roi/` - ROI缓存（每个ROI一次）
   - `POST /api/ocr/batch-detection/` - 批处理检测（所有目标齐全后一次）

### 性能监控

批处理完成后，console会输出性能指标：
```
⏱️ 批处理性能: 5个ROI, 耗时0.523s, 模式=cpu_concurrent, worker=3
```

---

## 📊 预期效果

### 成功集成的标志

1. ✅ 设置面板显示"批处理模式"选项
2. ✅ 批处理状态面板显示ROI缓存进度
3. ✅ Console日志显示ROI缓存和批处理触发
4. ✅ 所有目标检测完成后自动触发批处理
5. ✅ 批处理结果正确显示（拼接图、OCR文本、合格状态）
6. ✅ 性能指标显示处理时间和并发数

### 性能对比

| 场景 | 旧方案 | 批处理方案 | 提升 |
|------|--------|-----------|------|
| 5个ROI | ~1500ms | ~550ms | **2.7x** ⚡ |

---

## ✅ 集成检查清单

- [ ] 后端API测试通过（`/api/yolo/cache-roi/`）
- [ ] 前端Hook正确导入和使用
- [ ] 实时检测中调用 `cacheROI()`
- [ ] 批处理面板UI显示正确
- [ ] `onBatchComplete` 回调正确处理结果
- [ ] console日志显示完整流程
- [ ] 端到端测试通过
- [ ] 性能提升符合预期

---

**下一步**: 按照上述步骤修改 `OCRDetectionScreen.tsx`，建议先添加imports和Hook使用，然后逐步添加ROI缓存逻辑。
