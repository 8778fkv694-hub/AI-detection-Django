# 改善方案 — 近两日改动代码审查（2026-07-14 ~ 07-16）

**审查范围**：`0ae527b`（含）之后的 18 个提交 + 当前未提交的工作区改动，共 111 个文件（+6808 / -1409）。
**审查方式**：逐行扫描、删除行为审计、跨文件追踪、复用/简化/效率/架构 7 个角度，关键条目已在源码中人工复核确认。

> **修复状态（2026-07-16）**：P0-1、P0-2、P1-3、P1-5、E-1、E-3 已修复并通过全部 38 个后端测试 + TypeScript 编译（含为 P0-1 新增/修正的两个护栏测试）。P1-4 按需求方决定**不收紧阈值**：判定照常显示合格，但兜底通过的记录在保存时自动追加标注（reason 追加"⚠️ 一维码OCR数字兜底通过（条码未直读）: <数字串>"，reasonKeywords 追加"OCR兜底"，结构化字段 `barcode_analysis.results[].source='ocr_fallback'` 与 `ocr_fallback_count` 原本就在记录里）。E-2 与 M-1~M-7 未动。

---

## 一、正确性问题（建议尽快修复）

### P0-1 纯视觉目标误判不合格的修复只改了一半
**位置**：`backend/inspection/batch_detection_service.py:283-285`

OCR 执行后如果没有识别到文字，会硬性写入 `result['qualified'] = False`：

```python
if not result['ocr_text'].strip() or not detailed_results:
    result['qualified'] = False
    result['reason'] = 'OCR未识别到可复核文字'
```

提交 `2b4e16e` 在 `_validate_roi` 里加了 `requires_ocr_evidence` 门槛（没有关键词规则就不要求 OCR 证据），但验证只在 `apply_rules and target_configs` 同时成立时运行（第 321 行）并覆盖上面的硬置。两条漏网路径：

1. 工序配方可以配置 `batch_apply_rules = false`（`src/lib/stageRecipeApi.ts:98`，默认 `?? true` 但可关）。此时验证不运行，金色 Logo、出水口等纯视觉目标又会因"OCR 未识别到可复核文字"被判不合格 —— 正是 2b4e16e 想修的问题。
2. `target_configs` 为空 `{}` 时同样跳过验证，硬置直接生效。

**改法**：删掉第 283-285 行的硬置，OCR 证据要求完全交给 `_validate_roi` 里已有的 `requires_ocr_evidence` 逻辑（单一裁决点）。若担心 `apply_rules=False` 模式丢失提示，可只写 `result['reason']` 不动 `qualified`。

### P0-2（未提交）V4L2 缓冲区上限 512KB 会在大帧场景下持续丢帧
**位置**：`backend/inspection/v4l2_raw_reader.py:207`（工作区改动，`MAX_BUF` 4MB → 512KB）

读循环每轮都会把完整帧全部取走，`buf` 里最多只剩「一个不完整帧 + 一个 256KB 读块」，所以这个上限只会在**单帧体积超过约 256~512KB** 时触发。触发时若缓冲区开头就是 SOI（`soi == 0`），第一步裁剪不生效，直接 `buf = bytearray()` 把正在拼装的帧整个扔掉 —— 然后下一帧继续超限、继续被扔，流表现为持续卡死。高细节/高码率场景下 720p MJPEG 单帧到 300KB+ 并不罕见。

**改法**：`MAX_BUF` 回调到 1~2MB（防 OOM 依然充分），或者只在缓冲区里**找不到 SOI** 时才整体清空：

```python
if len(buf) > MAX_BUF:
    soi = buf.find(b'\xff\xd8')
    if soi > 0:
        buf = buf[soi:]
    elif soi == -1:
        buf = bytearray()
    # soi == 0：正在拼一个大帧，保留
```

### P1-3 启动检测循环时持全局锁加载模型，冻结所有窗口的画面叠加与轮询
**位置**：`backend/inspection/detection_loop.py:318-371`

`start_loop` 在持有 `DetectionLoopManager._lock` 的情况下调用 `pin_model()`（内部要排队进推理调度器并同步加载模型，Jetson 上 TensorRT 加载数秒）。而 `get_latest_boxes` / `get_latest_result` / `get_snapshot_jpeg`（458/467/476 行）每帧都要拿同一把锁 —— 打开第二个检测窗口或切换模型期间，**所有**流的 MJPEG 叠加和检测结果轮询整体冻结数秒。

**改法**：锁内只做账本登记（占住 loop_key、容量判断），把 `pin_model` + `DetectionLoop.start()` 挪到锁外执行，完成后再回锁提交；或者给每个 loop 用独立锁、管理器锁只保护字典。

### P1-4 一维条码"开放模式"OCR 数字兜底门槛过松，存在质检误放行
**位置**：`backend/inspection/batch_detection_service.py:530` 附近、`src/hooks/ocr/useBatchResultHandler.ts:114` 附近、`src/lib/barcode/barcodeAnalyzer.ts:196` 附近（同一逻辑三处）

未配置期望值时，只要 ROI 的 OCR 文本里出现**任意连续 4 位以上数字**（生产日期"2026"、型号、电话片段都算），就把"一维条码存在"判为通过。另外配置了期望值的 contains 模式是把 OCR 全文**剥掉所有非数字后拼接**再做子串匹配 —— 不同行的数字可能拼接出期望序列，同样有误放行面。

这是有意的产品取舍（避免拍摄质量问题误判），但建议收紧：

- 开放模式门槛提到 ≥8 位（贴近真实条码位数），或要求数字串独占一行（用 `ocr_detailed_results` 按行匹配而不是全文拼接）；
- contains 兜底按 OCR 明细逐条匹配，不做全文数字拼接；
- 兜底通过的结果 `source='ocr_fallback'` 已经带出来了 —— 前端结果页应对这类结果做醒目标记，提示人工复核。

### P1-5 拖动置信度滑块会反复销毁/重建检测循环
**位置**：`backend/inspection/detection_loop.py:258`（`_make_loop_key` 把 conf 编进 key）+ `src/hooks/ocr/useRealtimeDetectionLoop.ts:1260` 附近（effect 依赖 `detectionConfidence`）

置信度现在是循环身份的一部分，前端 effect 又依赖它，于是每次滑块变化都触发一次 stop（join 线程、unpin 模型）+ start（pin 模型、占推理调度槽），期间还会撞上 P1-3 的全局锁冻结。置信度只是过滤参数，不是模型状态。

**改法**：把 conf 移出 loop_key，恢复 `update_config` 原地更新；至少也要对滑块做 300ms+ 防抖再触发重启。

---

## 二、效率改善（Jetson 端收益明显）

### E-1（未提交）`serve_spa.py` 代理分块从 64KB 降到 8KB 只增加开销
**位置**：`serve_spa.py:182`

`read1()` 本身就是"有数据就返回"，不会等满缓冲区 —— 换成 `read1(8192)` 对 MJPEG 延迟没有额外收益，却让大响应（快照、批处理结果）多 8 倍的 Python 循环和 `wfile.write` 系统调用。**改为 `response.read1(64 * 1024)`**：低延迟语义不变，大传输恢复原吞吐。

### E-2 纯视觉目标被强制跑 OCR/条码，结果直接被丢弃
**位置**：`backend/inspection/batch_detection_service.py:243-251`

`should_run_ocr = is_selected_target or ...` 让每个选中目标都跑一遍 PaddleOCR（Jetson 上数百 ms~1s/ROI，且现在是串行），条码同理；而 P0-1 修复后这些无规则目标的 OCR/条码结果不参与判定。**改法**：检测触发条件与 `_validate_roi` 的证据需求对齐 —— 只有"有关键词规则、有条码规则（或其 OCR 兜底可能用到）"的 ROI 才跑对应检测。每次批处理可省下数秒。

### E-3 `serve_spa.py` 的 `_serving_gzip` 状态建议在代理路径显式复位
**位置**：`serve_spa.py:38-97`

当前服务器是 HTTP/1.0 每请求新建 handler，不会出问题；但如果将来打开 keep-alive（`protocol_version = 'HTTP/1.1'`），静态 gzip 请求残留的 `_serving_gzip=True` 会给后续代理响应错加 `Content-Encoding: gzip`，浏览器解压失败。防御性修复一行：`do_GET` 进入代理分支前置 `self._serving_gzip = False`。

---

## 三、可维护性改善（不改行为，防漂移）

### M-1 条码规则评估逻辑存在三份手写副本
后端 `_validate_roi`、前端 `useBatchResultHandler.evaluateConfigByRoi`、前端 `barcodeAnalyzer.matchRule` 三处实现了同一套「类型过滤 + 格式归一 + 文本匹配 + OCR 数字兜底」，格式映射表（`I25→ITF`）都各抄了一份。后端已经把逐规则结论放进 `result['barcode_match_details']`（含 matched/source/detectedText）—— **批处理路径的前端应直接消费后端结论**，删掉 `evaluateConfigByRoi`；单图路径保留 `matchRule` 一份即可。否则 P1-4 这类阈值调整永远要同步改三处，漏一处就出现"前端判合格、后端判不合格"的分裂。

### M-2 `targetRoi` 作用域谓词复制了 6 处
`cfg.get('targetRoi') in (None, '', 'all', label)` 在 `batch_detection_service.py` 出现 6 次（229、238、295、365、415、459 行附近），有的带 `enabled` 过滤有的不带，`scoped_barcode_configs` 还在检测和验证两个方法里各算一遍。提取一个模块级 helper：`_scoped(configs, label, check_enabled=True)`，算一次传下去。

### M-3 批处理三层签名 11 个位置参数
`process_batch → _process_rois → _process_single_roi` 三个几乎相同的签名各带 ~11 个参数，逐层转发；`_process_rois` 在去掉线程池后只剩一个裸 for 循环。**改法**：把请求选项打包成一个 dataclass（`BatchOptions`）一次传递，并内联删除 `_process_rois`。位置参数在 `enable_barcode` 之后全是新加的，再插一个参数就有静默错位绑定的风险。

### M-4 双窗口隔离的 storage-scope 逻辑在两个 store 逐字重复
`src/state/ocrDetectionStore.ts:20` 与 `src/state/ppeDetectionStore.ts:29` 的 `createScopedStorageName`/`buildScopeKey` 完全相同，仅前缀不同 —— 本次 6ce6a39 就是同一个 bug 打了两份相同补丁。提取到 `src/lib/scopedStorage.ts`，两个 store 传前缀调用。下次改 scope 规则时才不会只改一边、让双开 PPE 窗口重新互相踩持久化状态。

### M-5 detection_loop 管理器三套账本可以合成一套
`_owners`（loop→owner 集合）、`_owner_loops`（owner→loop）、`_anonymous_ref_counts` 三个结构描述同一事实，每个变更点都要成对维护，`_stop_loop_key_locked` 里的 `if self._owner_loops.get(owner) == loop_key` 防御检查说明两张表已被默认可能失配。owner 至多持一个 loop、loop 数量很小 —— 只保留 `_owner_loops`，需要反查时一行推导式，泄漏检测循环（= Jetson 上钉死一个模型）的风险面小得多。

### M-6 批处理失败兜底块在 useRealtimeDetectionLoop 复制两份
`src/hooks/ocr/useRealtimeDetectionLoop.ts` 约 875 与 1011 行，两个 `triggerBatchProcessing(false)` 之后的 5 连 set（unqualified/需复检/waiting_for_approval）逐字相同。收进一个局部 `failBatchToReview()`，或直接folded 进 `triggerBatchProcessing` 返回 false 的分支。

### M-7 条码评估页的"AI 图像质量分析"是随机数
`src/screens/GuidedBarcodeTestScreen.tsx:320` 附近，`performSmartAnalysis` 用 `Math.random()*100` 生成亮度/对比度/清晰度/噪点，再喂给约 60 行真实的建议分支（"图像亮度过低，建议增加光照…"）。同一张图每次跑出不同的调整建议，会误导产线调试。**要么删掉这一步和相关分支，要么用 canvas 真算这四个指标（约 15 行）**。

---

## 四、本次改动中做得对的（无需动作，留档）

- **CORS 收紧**（`settings.py`）：`CORS_ALLOW_ALL_ORIGINS=True` + credentials 的反射漏洞改为 localhost/私网正则白名单，正则锚定正确，172 网段范围（16-31）也写对了。
- **流状态错误清理**（`stream_api.py`）：恢复连接后清空 `last_error`/`error_count`，并配了迁移场景的回归测试。
- **流启动超时**（`stream_service.py`）：本地 3s / 网络 12s 区分，超时后显式停线程避免孤儿 reader，有测试覆盖。
- **ROI 缓存 owner 作用域**（`roi_cache.py`）：roi_id 加随机后缀防同毫秒覆盖、`clear_scoped` 保留旧语义兼容，重入锁说明准确。
- **模型固定 + 公平调度**（`yolo.py`）：pin 计数防 LRU 误淘汰、单 GPU 推理排队防 OOM，思路正确（注意 P1-3 是调用侧持锁问题，不是这里）。
- **serve_spa 预压缩 gzip + 哈希资产长缓存**：Jetson 上避免在线压缩，`Vary: Accept-Encoding` 与 Content-Type 修正都处理了。

---

## 五、建议执行顺序

| 优先级 | 条目 | 工作量 | 风险 |
|---|---|---|---|
| 立即 | P0-2（v4l2 MAX_BUF，尚未提交，改完再提交） | 5 分钟 | 低 |
| 立即 | P0-1（删除 OCR 硬置 qualified） | 15 分钟 + 回归测试 | 低 |
| 本周 | E-1（read1 分块恢复 64KB，尚未提交） | 2 分钟 | 低 |
| 本周 | P1-3（锁外加载模型） | 半天 | 中，需并发测试 |
| 本周 | P1-5（conf 移出 loop_key） | 2 小时 | 低 |
| 本周 | P1-4（兜底门槛收紧 + 前端标记） | 半天 | 需与质检需求方确认阈值 |
| 有空时 | E-2、M-1~M-7、E-3 | 各 0.5~2 小时 | 低 |

> 注：P0-2 和 E-1 都在未提交的工作区改动里，建议修正后再一并提交，避免把已知问题带进历史。
